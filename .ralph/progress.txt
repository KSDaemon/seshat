# Ralphy Progress Log
Started: понедельник, 30 марта 2026 г. 16:22:37 (EEST)
---

## Codebase Patterns
- `seshat-core` is the shared types crate with zero dependencies on other seshat crates — all shared enums/structs go here
- Re-exports in `lib.rs` flatten commonly used types for downstream ergonomics (e.g., `pub use dependency::DependencyDomain`)
- `seshat-scanner/src/manifest.rs` categorize functions use lowercased, hyphen-to-underscore normalized package names — match arms must use underscores not hyphens
- `seshat-detectors/src/dependency_usage.rs` classify functions use original package names (with hyphens) — match arms use the original crate name form
- `cargo fmt --all` must be run before committing — the project uses the default rustfmt settings
- Tests construct `ProjectFile` structs manually with explicit `language_ir` variants matching the language
- The `DependencyDomain` enum in `seshat-core/src/dependency.rs` is the single source of truth for dependency classification (12 variants), used by both scanner and detectors
- Storage repository pattern: trait in `repository/mod.rs`, SQLite impl in `repository/{entity}_repository.rs`, struct has `conn: Arc<Mutex<Connection>>` + `conn()` helper, free `row_to_{entity}()` function for row mapping
- Migrations use Refinery format: `V{N}__{description}.sql` in `crates/seshat-storage/migrations/`, embedded at compile time via `embed_migrations!("migrations")`
- JSON arrays stored as TEXT in SQLite, serialized/deserialized with serde_json in repository layer
- The `PackageRegistryClient` trait and types live in `seshat-scanner/src/registry/mod.rs`, concrete clients in `registry/{crates_io,npm,pypi}.rs`, storage repo lives in `seshat-storage/src/repository/package_metadata_repository.rs`
- Workspace deps defined in root `Cargo.toml` under `[workspace.dependencies]`, crate Cargo.toml uses `.workspace = true`
- The registry_mapping module in `seshat-scanner/src/registry/registry_mapping.rs` provides the three-tier dependency classification pipeline: (1) SQLite cache, (2) registry API fetch + cache, (3) hardcoded fallback. Use `classify_with_registry()` for full pipeline or individual mapping functions for targeted lookups.
- `Trend` enum lives in `seshat-core/src/knowledge.rs` (Rising/Stable/Declining/Unknown). `compute_trend()` in `seshat-detectors/src/confidence.rs` takes `&[Option<i64>]` file dates + config + now timestamp. P90 = sorted[ceil(N*0.9)-1]. Use `aggregate_findings(findings, config, file_dates, now)` with the `HashMap<String, Option<i64>>` parameter for trends.
- `aggregate_findings()` requires 4 parameters: findings, config, file_dates map, and now timestamp. Pass empty HashMap + 0 for now if trends are not needed.
- `AggregatedConvention::ext_data(&self, existing: Option<&Value>)` builds JSON with `{"trend": "...", "adoption_rate": ...}` merged with existing ext_data for KnowledgeNode construction.
- `ConventionDetector` trait has a default `detect_cross_file(&self, files: &[ProjectFile]) -> Vec<ConventionFinding>` method for cross-file analysis. The pipeline calls it once per detector after all per-file `detect()` calls. Only override if cross-file analysis is needed.
- Import resolution heuristics: Rust (`crate::`/`super::`/`self::` = internal), TS/JS (`./`/`../` = internal), Python (relative `.` or module path matches project file = internal). Use `is_internal_import()` and `import_resolves_to()` in `dependency_usage.rs`.
- `file_module_path()` converts a ProjectFile path to a canonical module path: Rust → `crate::mod::path`, TS/JS → `src/dir/file` (strip ext + /index), Python → `pkg.module` (strip .py + /__init__).
- `Function` struct has a `parameters: Vec<String>` field with `#[serde(default)]` for backward compat. Extracted per-language: Rust `extract_rust_parameters()` in `rust_parser.rs`, Python `extract_python_parameters()` in `python_parser.rs`, TS/JS shared `extract_js_ts_parameters()` + `find_arrow_or_function_expr()` in `parser/mod.rs`. Python excludes `self`/`cls`.
- Error handling detector: `classify_rust_error_lib()` maps import module root crate name to `RustErrorLib` enum (thiserror, anyhow, eyre, color_eyre, miette, snafu, error_stack, displaydoc). Uses underscore crate names (Rust import convention). Heuristic fallback: if no known lib but `derive(Error)` or `impl std::error::Error` found → Observation finding.
- Logging detector heuristic pattern: when no known library matches, `detect_heuristic_logging()` checks for (1) name-based: dep/import name containing log/logger/logging/trace/tracing/observ, (2) API shape: import with 2+ log-level names (info/debug/warn/error/fatal/trace). Known libs always take priority (heuristics only fire when `merged` evidence map is empty).
- Heuristic findings must use `KnowledgeNature::Observation` (never Convention/Decision) — this maps to lower weight/confidence in the aggregation pipeline.
- Test patterns detector heuristics: (1) `detect_config_file_framework()` matches `jest.config.*` → Jest, `vitest.config.*` → Vitest, `conftest.py` → pytest. (2) `detect_unknown_framework_fallback()` fires when a test file (by placement) has test functions but no framework identified. (3) `detect_heuristic_test_deps()` flags deps/imports with names containing test/mock/assert/spec. All emit Observation findings. Known frameworks always take priority.
- Dependency usage detector heuristic: `classify_heuristic_domain()` maps unrecognized package names to domains via keyword matching: test/mock → Testing, log/trace → Logging, http/web/api/rest → Http, sql/db/orm → Database, cli/command/arg → Cli, serial/json/yaml/proto → Serialization, valid/schema → Validation. Only fires for packages not in the known-library classify lists. Emits Observation findings.
- `is_heuristic_test_dep()` checks if a dep name contains testing keywords AND is NOT already a known test framework (guards against double-counting). For Rust, `is_known_rust_test_dep()` covers proptest/quickcheck/rstest/criterion/test-case/mockall/wiremock/assert_cmd/assert_fs/insta.
- `JavaScriptIR::module_system` is populated by the parser's `detect_module_system()` which resolves mixed ESM+CJS → ESM. To detect mixed files in detectors, check for CJS signals (`has_module_exports` or non-empty `require_calls`) when `module_system == ESM`.

## 2026-03-30 16:30 (EEST) - US-001
- What was implemented: Unified DependencyDomain taxonomy — single enum in seshat-core replacing both the 8-variant DependencyDomain in seshat-detectors and the 12-variant DependencyCategory in seshat-scanner
- Files changed:
  - `crates/seshat-core/src/dependency.rs` (NEW) — 12-variant DependencyDomain enum with Serialize/Deserialize, serde rename_all snake_case, as_str() method
  - `crates/seshat-core/src/lib.rs` — added `pub mod dependency` and `pub use dependency::DependencyDomain`
  - `crates/seshat-detectors/src/dependency_usage.rs` — removed old 8-variant DependencyDomain enum and impl block, updated classify_*() to import from seshat_core, split WebFramework from Http (actix-web, axum, warp, rocket, tide, poem → WebFramework; reqwest, hyper, ureq → Http), added Crypto domain for Rust, fixed test assertions
  - `crates/seshat-scanner/src/manifest.rs` — removed DependencyCategory enum, DeclaredDependency.category now uses DependencyDomain, categorize_*_dep() return DependencyDomain, Async→AsyncRuntime rename, all test assertions updated
  - `crates/seshat-scanner/src/lib.rs` — re-export list updated: removed DependencyCategory, added categorize_dependency
- **Learnings for future iterations:**
  - The scanner's categorize functions normalize names (lowercase + hyphen→underscore) BEFORE matching, so match arms use underscored names. The detectors' classify functions match against original package names (with hyphens). These two conventions differ intentionally.
  - Web frameworks (express, flask, django, axum, actix-web, rocket, etc.) are now `WebFramework`, HTTP clients (reqwest, axios, httpx) are `Http`. This is a deliberate split from the old detectors code which lumped them together.
  - `DependencyCategory::Async` was renamed to `DependencyDomain::AsyncRuntime` for consistency with the original detectors enum naming.
  - `cargo fmt` reformatted many files beyond our changes — pre-existing format drift existed in the codebase. Run `cargo fmt --all` before committing.
---

## 2026-03-30 17:15 (EEST) - US-002
- What was implemented: Package registry metadata foundation — SQLite migration, PackageRegistryClient trait, PackageMetadata/RegistryError types, storage repository with upsert/query/delete_stale, ureq dependency added
- Files changed:
  - `crates/seshat-storage/migrations/V2__package_metadata.sql` (NEW) — creates package_metadata table with (name, registry) PK, categories/keywords as JSON TEXT, fetched_at INTEGER, plus registry and fetched_at indexes
  - `crates/seshat-scanner/src/registry.rs` (NEW) — Registry enum (CratesIo/Npm/PyPI), PackageMetadata struct, RegistryError (thiserror), PackageRegistryClient trait with fetch_metadata(), CACHE_TTL_SECS constant (30 days = 2_592_000 seconds)
  - `crates/seshat-storage/src/repository/package_metadata_repository.rs` (NEW) — PackageMetadataRow struct, SqlitePackageMetadataRepository with upsert (INSERT ON CONFLICT UPDATE), get, get_by_registry, delete_stale; 7 unit tests
  - `crates/seshat-storage/src/repository/mod.rs` — added PackageMetadataRepository trait and module registration
  - `crates/seshat-storage/src/lib.rs` — re-exports for new types
  - `crates/seshat-storage/src/db.rs` — updated migration test to verify package_metadata table and indexes
  - `crates/seshat-scanner/src/lib.rs` — added registry module and re-exports
  - `crates/seshat-scanner/Cargo.toml` — added ureq.workspace = true
  - `Cargo.toml` — added ureq = "3" to workspace dependencies
  - `Cargo.lock` — ureq and transitive deps
- **Learnings for future iterations:**
  - Migration numbering: V2 is used for package_metadata (not V3 as originally in the AC) because US-005 needs V2 for file dates. The PRD AC said "V3" but the ordering note says V2 must come before V3. Since this was implemented first, it got V2.
  - The storage crate uses `ON CONFLICT ... DO UPDATE SET` (SQLite upsert) for idempotent inserts — this is the standard pattern for cache-style tables.
  - `PackageMetadataRow` is a plain storage-layer struct (in seshat-storage), while `PackageMetadata` is the scanner-layer domain struct (in seshat-scanner). They map 1:1 but live in different crates to respect crate boundaries.
  - The `Registry` enum has `as_str()` → `"crates_io"/"npm"/"pypi"` and `FromStr` for roundtripping to/from the database TEXT column.
  - `ureq` v3 is used (not v2) — it pulls in rustls for TLS. No feature flags needed for basic HTTP GET.
---

## 2026-03-30 18:00 (EEST) - US-003
- What was implemented: Concrete PackageRegistryClient implementations for all three registries (crates.io, npm, PyPI). Converted `registry.rs` file to `registry/` module directory.
- Files changed:
  - `crates/seshat-scanner/src/registry.rs` → `crates/seshat-scanner/src/registry/mod.rs` — converted to module directory, added `pub mod crates_io/npm/pypi`, updated module-level doc links
  - `crates/seshat-scanner/src/registry/crates_io.rs` (NEW) — `CratesIoClient` struct with `fetch_metadata()` impl, parses `categories[].slug` and `keywords[]` from crates.io API JSON, 7 unit tests
  - `crates/seshat-scanner/src/registry/npm.rs` (NEW) — `NpmClient` struct with `fetch_metadata()` impl, parses `keywords[]` from npm registry JSON (no structured categories), 8 unit tests
  - `crates/seshat-scanner/src/registry/pypi.rs` (NEW) — `PyPIClient` struct with `fetch_metadata()` impl, parses `classifiers[]` (as categories) and comma-separated `keywords` string from PyPI JSON API, 8 unit tests
  - `crates/seshat-scanner/src/lib.rs` — added re-exports for `CratesIoClient`, `NpmClient`, `PyPIClient`
  - `.ralph/prd.json` — US-003 `passes: true`
- **Learnings for future iterations:**
  - ureq v3 API: `Agent::config_builder().timeout_global(Some(Duration::from_secs(5))).build()` creates config, then `.into()` to get Agent. Requests: `agent.get(url).header(k,v).call()?.into_body().read_to_string()?`
  - ureq v3 `Error::StatusCode(u16)` matches HTTP status errors directly — use literal pattern `Error::StatusCode(404)` not a guard, or clippy emits `redundant_guards` warning
  - For testing without real HTTP: extract JSON parsing into standalone `parse_*_response(name, json_str) -> Result<PackageMetadata, RegistryError>` functions, test those with realistic JSON fixtures. Client struct holds configurable `base_url` (test-only constructor via `#[cfg(test)]`).
  - PyPI keywords come as a single comma-separated string (not an array), so must be split + trimmed. PyPI classifiers serve the same purpose as crates.io categories. npm has no structured categories at all.
  - User-Agent header uses `concat!("seshat/", env!("CARGO_PKG_VERSION"))` to embed version at compile time per registry API policies.
  - When converting a single file module (`foo.rs`) to a directory module (`foo/mod.rs`), module-level doc links like `[`MyType`]` may break if the type is now in a submodule — use `[`submod::MyType`]` for rustdoc resolution.
---

## 2026-03-30 18:50 (EEST) - US-004
- What was implemented: Category-to-DependencyDomain mapping rules (~30 total) and three-tier lookup function for classifying dependencies via registry metadata.
- Files changed:
  - `crates/seshat-scanner/src/registry/registry_mapping.rs` (NEW) — ~30 mapping rules for crates.io categories, PyPI classifiers, and keywords to DependencyDomain. Three-tier lookup: (1) SQLite cache → (2) registry API fetch + cache → (3) hardcoded fallback. ClassificationConfidence enum (Registry, Fallback). ClassificationResult struct. Unit tests for all three tiers + mapping rules (~40 tests total).
  - `crates/seshat-scanner/src/registry/mod.rs` — added `pub mod registry_mapping`
  - `crates/seshat-scanner/src/lib.rs` — added re-exports for ClassificationConfidence, ClassificationResult, classify_with_registry, infer_domain_from_metadata, map_crates_io_category, map_keyword, map_pypi_classifier
  - `.ralph/prd.json` — US-004 `passes: true`
- **Learnings for future iterations:**
  - The three-tier lookup short-circuits: if the hardcoded list already knows the package (domain != Unknown), it returns immediately without touching cache or network. This means known-library matches get `Fallback` confidence (not `Registry`) which is correct — they're the most reliable.
  - crates.io categories use slug format with `::` nesting (e.g., `"web-programming::http-client"`). PyPI classifiers use ` :: ` delimiters (e.g., `"Framework :: Flask"`). npm has NO structured categories — only keywords.
  - For mock testing the three-tier lookup, use `std::cell::RefCell<Vec<PackageMetadataRow>>` for the mock repo to allow interior mutability (needed because `upsert()` takes `&self` not `&mut self`).
  - The `PackageMetadataRepository` trait lives in seshat-storage, but the `PackageRegistryClient` trait lives in seshat-scanner. The registry_mapping module depends on both — this works because seshat-scanner already depends on seshat-storage.
  - When `cargo fmt` reorders imports (e.g., `CACHE_TTL_SECS` before `PackageMetadata`), it changes the use statement ordering. Always run `cargo fmt` after editing imports.
- gix v0.72 tree diff API: call `tree.changes()?` to get `Platform`, then `platform.options(|opts| { opts.track_path(); })` to enable path tracking, then `platform.for_each_to_obtain_tree(&other_tree, |change| { ... })`. The `change.location()` method returns the file path. Do NOT chain `.track_path()` on Platform — it's a method on `gix::diff::Options` accessed via `.options()`.
- `collect_git_file_dates()` lives in `seshat-scanner/src/git_dates.rs`, returns `HashMap<PathBuf, i64>` (relative paths → Unix timestamps). For non-git dirs or empty repos, returns empty map without error. Integrated in orchestrator Step 1b, passed to `file_ir_repo.upsert()` as separate `last_commit_date: Option<i64>` parameter (NOT stored in ProjectFile struct).
- The Rust 2024 edition formatter reorders `use` imports alphabetically (types before functions). This means `cargo fmt` will reformat files that were formatted under older rules — always run `cargo fmt --all` before committing.

## 2026-03-30 19:30 (EEST) - US-005
- What was implemented: Fixed git file dates collection (gix integration) — the code was already scaffolded but had a compilation error due to incorrect gix v0.72 tree diff API usage. Fixed `track_path()` call (was chained on Platform, should be set via `.options()` on diff Options), and fixed `change.location` field access to use `change.location()` method. Also removed unused variable in orchestrator and ran `cargo fmt` to fix pre-existing format drift.
- Files changed:
  - `crates/seshat-scanner/src/git_dates.rs` — Fixed tree diff API: replaced `.track_path().for_each_to_obtain_tree()` with `.options(|opts| { opts.track_path(); }).for_each_to_obtain_tree()`, changed `change.location.to_string()` to `change.location().to_string()`
  - `crates/seshat-scanner/src/orchestrator.rs` — Removed unused `file_path_str` variable in Step 5 loop
  - Multiple files — `cargo fmt --all` reformatted import ordering (Rust 2024 edition alphabetizes use items)
  - `.ralph/prd.json` — US-005 `passes: true`
- **Learnings for future iterations:**
  - gix v0.72 tree diff API: `track_path()` is a method on `gix::diff::Options`, NOT on the diff `Platform`. Access it via `platform.options(|opts| { opts.track_path(); })` BEFORE calling `for_each_to_obtain_tree()`.
  - The `Change` enum in gix 0.72 has a `location()` method (not a direct field access `change.location`) to get the file path as `&BStr`.
  - Previous iterations scaffolded US-005 code that didn't compile — always verify compilation before marking stories as done.
  - `cargo fmt` on Rust 2024 edition reorders imports differently than older editions. Expect widespread reformatting on first `cargo fmt --all` run.
---

## 2026-03-30 20:10 (EEST) - US-006
- What was implemented: Convention trend computation with P90 percentile — Trend enum, compute_trend() function, extended aggregate_findings() with file dates parameter, ext_data helper for KnowledgeNode construction.
- Files changed:
  - `crates/seshat-core/src/knowledge.rs` — Added Trend enum (Rising/Stable/Declining/Unknown) with Serialize/Deserialize, serde rename_all snake_case, as_str(), Display, FromStr implementations, 4 tests
  - `crates/seshat-core/src/config.rs` — Extended DetectionConfig with trend_rising_days: u32 (default 90) and trend_stable_days: u32 (default 365), updated test
  - `crates/seshat-core/src/lib.rs` — Added Trend to re-exports
  - `crates/seshat-detectors/src/confidence.rs` — Added compute_trend() with P90 percentile (sort dates ascending, index=ceil(N*0.9)-1), extended AggregatedConvention with trend field, added ext_data() helper method for building KnowledgeNode ext_data JSON with trend+adoption_rate. Updated aggregate_findings() to accept file_dates HashMap and now timestamp. 13 new tests for compute_trend boundaries + 2 for ext_data + 1 for aggregate with trends.
  - `crates/seshat-detectors/src/lib.rs` — Added compute_trend to re-exports
  - `crates/seshat-detectors/Cargo.toml` — Added serde_json.workspace = true dependency
  - `.ralph/prd.json` — US-006 `passes: true`
- **Learnings for future iterations:**
  - `seshat-detectors` did not previously depend on `serde_json` — it was needed for the ext_data helper that builds JSON values. Added via `serde_json.workspace = true`.
  - P90 percentile formula: sort ascending, index = ceil(N * 0.9) - 1, clamped to [0, N-1]. For N=1, index=0 (the single element). For N=10, index=8.
  - `aggregate_findings()` now requires 4 parameters (findings, config, file_dates, now). Existing callers must pass empty HashMap and 0 for now if trends are not relevant. All existing tests were updated.
  - The Trend enum uses `< threshold` semantics (not `<=`): e.g. age_days < 90 → Rising, age_days < 365 → Stable, else Declining. At exactly 90 days, trend is Stable (not Rising).
---

## 2026-03-30 21:25 (EEST) - US-007
- What was implemented: Wrapper/facade convention detection via import graph analysis. Extended `ConventionDetector` trait with default `detect_cross_file()` method, updated pipeline to call it after per-file detection, implemented `DependencyUsageDetector.detect_cross_file()` with full cross-file wrapper detection algorithm.
- Files changed:
  - `crates/seshat-detectors/src/trait_def.rs` — Added `detect_cross_file(&self, files: &[ProjectFile]) -> Vec<ConventionFinding>` default method to `ConventionDetector` trait (backward-compatible, returns empty Vec by default)
  - `crates/seshat-detectors/src/pipeline.rs` — Updated `run_detectors()` to run Phase 2 cross-file detection after Phase 1 per-file detection, merging findings by file_path into existing `DetectorResults`. Panic safety preserved.
  - `crates/seshat-detectors/src/dependency_usage.rs` — Added ~300 lines: `detect_cross_file()` override, `detect_wrapper_facades()` core algorithm, `is_internal_import()`, `file_module_path()`, `import_resolves_to()`, `normalize_relative_path()`, `root_package()` helper functions. 13 new unit tests: Python wrapper (5 consumers, 2 direct = 2 violations), TS wrapper (4 consumers, 1 direct = 1 violation), no wrapper, minority usage, single file, wrapper not flagged, root_package for all langs, is_internal for all langs, file_module_path for all langs, normalize path, empty files.
  - `.ralph/prd.json` — US-007 `passes: true`
- **Learnings for future iterations:**
  - The `ConventionDetector` trait now has TWO analysis methods: `detect(&self, file)` for per-file analysis and `detect_cross_file(&self, files)` for cross-file analysis. Only `detect()` is required; `detect_cross_file()` has a default empty implementation.
  - The pipeline's Phase 2 (cross-file) runs sequentially per detector, NOT in parallel via rayon. This is intentional because cross-file analysis needs the full file list and doesn't parallelize at the file level.
  - Import resolution is language-specific and purely structural (no hardcoded directory names). For Python, internal import detection needs the set of project file paths to compare against module paths (e.g., `myapp.utils` resolves to `myapp/utils.py`).
  - Wrapper detection threshold: `wrapper_consumer_count > direct_user_count` (strict greater-than). If exactly half use the wrapper, no convention is established.
  - `root_package()` handles scoped npm packages (`@scope/name`) — two slashes needed to find the package boundary. Python uses `.` separator, Rust uses `::`.
  - clippy requires `is_none_or()` instead of `map_or(true, ...)` on Option types (`unnecessary_map_or` lint).
  - clippy catches `PathBuf::from("...")` in assert comparisons as `cmp_owned` — use `Path::new("...")` or `.as_path()` for zero-allocation comparison.
---

## 2026-03-30 22:45 (EEST) - US-008
- What was implemented: Added `parameters: Vec<String>` field to `Function` struct and parameter name extraction to all 4 tree-sitter parsers (Rust, TypeScript, JavaScript, Python).
- Files changed:
  - `crates/seshat-core/src/ir.rs` — Added `parameters: Vec<String>` field to `Function` struct with `#[serde(default)]` for backward compatibility. Updated serialization roundtrip test.
  - `crates/seshat-scanner/src/parser/mod.rs` — Added shared `extract_js_ts_parameters()` function (handles `formal_parameters` with `identifier`, `required_parameter`, `optional_parameter`, `assignment_pattern`, `rest_pattern` node types). Added `find_arrow_or_function_expr()` helper. Updated `extract_function_declaration()` and `extract_exported_lexical()` to pass parameters.
  - `crates/seshat-scanner/src/parser/rust_parser.rs` — Added `extract_rust_parameters()` (walks `parameters` child, extracts `identifier` from `parameter` pattern, skips `self_parameter`). Updated `extract_function()`. 5 new tests.
  - `crates/seshat-scanner/src/parser/typescript_parser.rs` — Updated `extract_lexical_functions()` to use `find_arrow_or_function_expr()` + `extract_js_ts_parameters()`. 6 new tests.
  - `crates/seshat-scanner/src/parser/javascript_parser.rs` — Updated `extract_top_level_declaration()` to use `find_arrow_or_function_expr()` + `extract_js_ts_parameters()`. 7 new tests.
  - `crates/seshat-scanner/src/parser/python_parser.rs` — Added `extract_python_parameters()` (handles `identifier`, `typed_parameter`, `default_parameter`, `typed_default_parameter`, `list_splat_pattern`, `dictionary_splat_pattern`; excludes `self`/`cls`). 9 new tests.
  - `crates/seshat-detectors/src/{naming,test_patterns,error_handling,export_patterns}.rs` — Updated all test helpers and inline `Function` constructions with `parameters: vec![]`.
  - `crates/seshat-storage/src/ir_serialization.rs` — Updated 3 test `Function` constructions with `parameters: vec![]`.
- **Learnings for future iterations:**
  - Tree-sitter Rust: `function_item` has `parameters` child, each `parameter` has a `pattern` field (accessible via `child_by_field_name("pattern")`) that is typically an `identifier`.
  - Tree-sitter TS: uses `formal_parameters` (not `parameters`), with `required_parameter` and `optional_parameter` child types that have `identifier` children.
  - Tree-sitter JS: uses `formal_parameters`, simpler than TS — parameters are direct `identifier` children. Default params are `assignment_pattern` with left child as the name.
  - Tree-sitter Python: uses `parameters`, with `identifier` for simple params, `typed_parameter`/`typed_default_parameter`/`default_parameter` for complex params, `list_splat_pattern`/`dictionary_splat_pattern` for *args/**kwargs.
  - Python parser only extracts top-level `function_definition` nodes, NOT methods inside class bodies. Tests for `self`/`cls` exclusion must use top-level functions that happen to have `self`/`cls` params.
  - `#[serde(default)]` on the new field ensures backward compatibility — existing serialized data without the field deserializes to empty vec.
  - clippy `single_match` lint: use `if` instead of `match` when only one arm does work and the other is `_ => {}`.
---

## 2026-03-30 23:30 (EEST) - US-009
- What was implemented: Parameter naming analysis in the naming detector — analyzes function parameter case patterns (snake_case/camelCase) per language with language-aware weighting (Rust = Observation/lower weight, JS/TS/Python = Convention/higher weight).
- Files changed:
  - `crates/seshat-detectors/src/naming.rs` — Added `expected_parameter_case()` function (snake_case for Rust/Python, camelCase for JS/TS), `detect_parameter_naming()` function that collects all params from all functions in a file, classifies them against expected convention, and produces a ConventionFinding. Called from all 4 per-language detect functions. Updated module doc comment. Added 13 new tests: Rust snake_case params follow, Rust camelCase params deviate, TS camelCase params follow, TS snake_case params deviate, JS camelCase params follow, JS snake_case params deviate, Python snake_case params follow, Python camelCase params deviate, TS mixed param naming (67%), no params produces no finding, single-word params treated as conforming, evidence capped at 10. Added `func_with_params()` test helper.
  - `.ralph/prd.json` — US-009 `passes: true`
- **Learnings for future iterations:**
  - Parameter naming follows the same expected case as function naming: snake_case for Rust/Python, camelCase for JS/TS. The `expected_parameter_case()` mirrors `expected_function_case()`.
  - Language-aware weighting is handled by the existing `finding_nature()` function — Rust gets `KnowledgeNature::Observation` (lower weight, compiler-enforced), JS/TS/Python get `KnowledgeNature::Convention` (higher weight, community-driven).
  - Parameters are collected as `(param_name, function_line)` pairs using `flat_map` over functions and their parameters — the function's line number is used as the evidence line since parameters don't have their own line numbers in the IR.
  - The existing `classify_names()` utility works perfectly for parameter classification — it already handles single-word ambiguity via `matches_expected()` and excludes `Unknown` patterns from both conforming and non-conforming counts.
   - Files with functions that have no parameters correctly produce no "Parameter naming" finding since the params collection is empty and the function returns early.
---

## 2026-03-30 24:15 (EEST) - US-010
- What was implemented: Heuristic fallbacks for error handling and logging detectors — unknown libraries classified via structural signals (derive(Error), impl Error, name patterns, API shape).
- Files changed:
  - `crates/seshat-detectors/src/error_handling.rs` — Added `RustErrorLib` variants for eyre, color-eyre, miette, snafu, error-stack, displaydoc. Added `classify_rust_error_lib()` function for known library classification via import module root crate name. Heuristic: when no known lib matched, checks for `derive(Error)` or `impl std::error::Error` → Observation finding. Extended context wrapping to detect eyre::WrapErr. Generalized conflict detection to use `distinct_libs` count (not just thiserror+anyhow). Added MAX_EVIDENCE constant. 12 new tests: 6 for new known libraries, 4 for heuristic fallback (derive/impl/priority/no-match), 1 for Python name heuristic, 1 for eyre context wrapping.
  - `crates/seshat-detectors/src/logging_observability.rs` — Added heuristic detection infrastructure: `LOGGING_NAME_HINTS` (log/logger/logging/trace/tracing/observ), `LOG_API_NAMES` (info/debug/warn/error/fatal/trace), `is_heuristic_logging_name()`, `has_logging_api_shape()`, `detect_heuristic_logging()`. Integrated into all 3 per-language detect functions (Rust, JS/TS, Python) — heuristics fire only when no known library is found in merged evidence map. 9 new tests: name-based dep/import/observ, known-lib priority, unrelated dep, API shape (sufficient/insufficient), helper function assertions.
  - `.ralph/prd.json` — US-010 `passes: true`
- **Learnings for future iterations:**
  - `classify_rust_error_lib()` uses underscore crate names (e.g., `color_eyre`, `error_stack`) because Rust import paths use underscores, not hyphens. The `RustErrorLib::as_str()` returns the hyphenated form for human-readable descriptions.
  - The `has_thiserror_derive` check must be gated on `has_thiserror` (thiserror import present) — otherwise `derive(Error)` from ANY proc-macro crate would be misattributed to thiserror. The heuristic path handles the case where derive(Error) appears without thiserror.
  - Logging heuristics use a layered approach: name-based first (cheap), then API shape (looks at imported names). At least 2 log-level names must be imported to trigger the API shape heuristic — a single `info` import could be coincidental.
  - Known-library findings always take priority over heuristic findings by short-circuiting: heuristics are only invoked when the known-library evidence map (`merged`) is empty. This is the simplest and most correct approach — no need for complex priority merging.
   - `KnowledgeNature::Observation` is the correct nature for all heuristic findings. The aggregation pipeline maps this to lower confidence/weight automatically.
---

## 2026-03-30 25:00 (EEST) - US-011
- What was implemented: Heuristic fallbacks for test patterns and dependency usage detectors — config file detection, unknown framework fallback, dependency name heuristics for both detectors.
- Files changed:
  - `crates/seshat-detectors/src/test_patterns.rs` — Added 3 heuristic mechanisms: (1) `detect_config_file_framework()` matches jest.config.*/vitest.config.*/conftest.py to infer framework, (2) `detect_unknown_framework_fallback()` emits Observation when test file has test functions but no identifiable framework, (3) `detect_heuristic_test_deps()` flags deps/imports with names containing test/mock/assert/spec. Added `is_heuristic_test_dep()`, `is_known_rust_test_dep()` helpers. Integrated into all 3 per-language detect functions. Added `HashSet` import. 16 new tests: config file detection (jest/vitest/conftest/none), config file findings (jest/vitest/conftest), unknown framework fallback (JS/Python), known framework priority, heuristic dep detection (test/mock/assert/spec), known framework not flagged, no heuristic for unrelated, helper function assertions.
  - `crates/seshat-detectors/src/dependency_usage.rs` — Added `HEURISTIC_DOMAIN_KEYWORDS` constant mapping keyword groups to domains (test/mock → Testing, log/trace → Logging, http/web/api/rest → Http, sql/db/orm → Database, cli/command/arg → Cli, serial/json/yaml/proto → Serialization, valid/schema → Validation). Added `classify_heuristic_domain()` function that only fires for packages not in the known-library list. Integrated into `detect()` method after known-library classification. 15 new tests covering all domain keywords, known-package bypass, unrelated package, observation nature, known-library priority, multiple unknown deps, case insensitivity.
  - `.ralph/prd.json` — US-011 `passes: true`
- **Learnings for future iterations:**
  - The JS/TS `detect_js_ts_framework()` function infers Jest from global function names (describe/it/test), which means the unknown framework fallback ONLY fires for test files whose functions don't match these exact names. Functions like `test_something` won't trigger framework inference but will trigger the fallback.
  - `detect_config_file_framework()` uses filename-based matching (not content analysis). For pyproject.toml pytest config detection, you'd need to parse the file content (deferred). `conftest.py` is used as a pytest indicator since it's exclusively a pytest concept.
  - The dependency usage heuristic uses a `classified_packages` HashSet built from known-library domain groups to avoid emitting heuristic findings for packages already classified. This avoids double-counting but means the `classify_heuristic_domain()` function also independently checks `classify_domain()` — belt-and-suspenders approach.
  - clippy `question_mark` lint: `if x.is_none() { return None; }` should be replaced with `x?;` for Option returns.
  - `cargo fmt` reformats `assert!(multi.line.expression)` into multi-line `assert!(\n    multi\n        .line\n        .expression\n)` — existing test code may get reformatted.
---

## 2026-03-30 25:30 (EEST) - US-012
- What was implemented: Made the export patterns detector read the `JavaScriptIR::module_system` field (previously dead code) to emit module system findings for JavaScript files.
- Files changed:
  - `crates/seshat-detectors/src/export_patterns.rs` — Added `ModuleSystem` import. Added `detect_module_system_finding()` function that reads `js_ir.module_system` and emits: pure ESM finding (Observation, follows convention), pure CommonJS finding (Observation, follows convention), mixed ESM+CJS finding (Observation, does NOT follow convention). Mixed detection: if `module_system` is ESM but `has_module_exports` or `require_calls` is non-empty → mixed. Called from `detect_javascript()` after module.exports detection. Added 5 new tests: pure ESM, pure CJS, mixed ESM+CJS (module.exports), mixed ESM+require_calls, Unknown module system (no finding).
  - `.ralph/prd.json` — US-012 `passes: true`
- **Learnings for future iterations:**
  - The JS parser's `detect_module_system()` resolves mixed ESM+CJS to `ModuleSystem::ESM` (ESM takes precedence). To detect mixed files in the export detector, check for CJS signals (`has_module_exports` or non-empty `require_calls`) when `module_system == ESM`.
  - `ModuleSystem::Unknown` means no import/export/require signals were detected — the detector correctly emits no module system finding for these files.
  - All module system findings use `KnowledgeNature::Observation` — they describe what IS, not what SHOULD BE. Mixed ESM+CJS has `follows_convention: false` to flag it as a potential issue.
---

## 2026-03-30 26:00 (EEST) - US-013
- What was implemented: Branch code review and quality gate — comprehensive audit of all changes on chore/competitive-analysis-retrofit branch.
- Quality checks passed:
  - `cargo fmt --all --check` — clean
  - `cargo clippy --all-targets --all-features -- -D warnings` — clean
  - `cargo test --workspace` — all 791+ tests pass
  - `cargo doc --no-deps --document-private-items` — clean after fixes
- Issues found and fixed:
  - 3 broken rustdoc intra-doc links: `detect_cross_file`, `JavaScriptIR::module_system`, `collect_git_file_dates` — converted to backtick-quoted plain text since they reference cross-crate items
  - 1 `.unwrap()` in library code (`dependency_usage.rs:495`, `strip_prefix("super::").unwrap()`) — refactored to `if let Some(suffix) = import_module.strip_prefix("super::")`
  - 7 unnecessary `.clone()` calls in `registry_mapping.rs` — eliminated by reordering `infer_domain_from_metadata()` before moving fields into cache row (tier 2), and taking `PackageMetadataRow` by value in `row_to_metadata()` (tier 1)
  - Missing `#[tracing::instrument]` on `aggregate_findings()` — added with `skip_all` and `n_findings` field
- Audit results (no action needed):
  - No `unsafe` blocks in new code
  - No `.expect()` in library code (only in tests)
  - No remnants of old `DependencyCategory` or duplicate `DependencyDomain` enums
  - Crate boundaries respected: no SQL in scanner/detectors, no HTTP in detectors/graph
  - All tests deterministic: no real HTTP calls, no system clock usage
  - Error handling uses thiserror + `?` propagation throughout
  - Function signatures use `&str`/`&[T]`/`&Path` — no unnecessary `String` ownership
- Files changed:
  - `crates/seshat-detectors/src/dependency_usage.rs` — eliminated `.unwrap()` via `if let Some(suffix)`
  - `crates/seshat-detectors/src/export_patterns.rs` — fixed broken doc link
  - `crates/seshat-detectors/src/confidence.rs` — added `#[tracing::instrument]` to `aggregate_findings()`
  - `crates/seshat-scanner/src/registry/registry_mapping.rs` — eliminated 7 `.clone()` calls via reordering and by-value `row_to_metadata()`
  - `crates/seshat-storage/src/repository/mod.rs` — fixed broken doc link
- **Learnings for future iterations:**
  - When referencing cross-crate items in rustdoc, use backtick-quoted plain text (`` `Foo::bar` ``) instead of intra-doc link syntax (`[`Foo::bar`]`) — intra-doc links only resolve within the same crate.
  - Always run `cargo doc --no-deps --document-private-items` as part of CI — broken doc links are easy to miss and accumulate.
  - To avoid `.clone()` when caching API responses: infer/compute results from the borrowed data first, then move fields into the cache struct.
  - `row_to_metadata()` can take ownership of `PackageMetadataRow` since the row is only used to construct `PackageMetadata` and is not needed afterward.
---
