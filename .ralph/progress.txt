# Ralphy Progress Log
Started: воскресенье, 10 мая 2026 г. 11:54:33 (EEST)
---

## Codebase Patterns
- Edition 2024, Rust 1.85+. `let-else`, `..=` ranges, and similar modern syntax are fine.
- `seshat-graph/src/dependencies.rs` already has a `SuffixIndex` (O(1) reverse-suffix lookup) and an `infer_package_root` heuristic for cross-crate guards. Reuse instead of rolling new ones.
- IR `path` is stored as an absolute `PathBuf`; `target_path` arguments to public APIs are typically relative — always normalise via `normalize_path` + `suffix_matches_at_boundary` before comparing.
- For new pure-graph utilities (BFS, adjacency builders), construct synthetic `HashMap<String, Vec<…>>` directly in unit tests; no DB / IR roundtrip needed. The repo-level `test_helpers::{test_conn, insert_ir}` is for integration-style tests.
- Rust style: snake_case fns/params, PascalCase types, `expect("message")` over `unwrap()` in tests, doc comments on every `pub` item with `///`.
- `DependentEntry` and similar serde types use `#[serde(default)]` on newly-added fields for forward compatibility within the same schema version.
- Clippy is run as `cargo clippy --all-targets -- -D warnings` and CI gates on it — no warnings tolerated.

---

## 2026-05-10 12:23 (EEST) - US-001
- Built the cycle-safe BFS core for transitive dependents in
  `crates/seshat-graph/src/dependencies.rs`:
  - Added `pub const MAX_DEPENDENTS: usize = 500`.
  - Added `pub struct ReverseEdge { from, import_names, line }` and
    `pub struct TransitiveResult { entries, truncated }`.
  - Made `SuffixIndex` (and `SuffixIndex::build`) public so callers can
    amortise the suffix index across many resolutions (US-002 will lean on
    this for `query_dependencies_batch`).
  - Extended `DependentEntry` with `depth: u32` and `via: Vec<String>`
    (`#[serde(default)]` to keep envelope compatibility). The single
    existing construction site (`build_dependents`) populates
    `depth: 1, via: vec![]`.
  - Extracted shared `package_boundary_ok` helper; reused by the legacy
    `import_resolves_to_target` (single-target) and the new
    `pub(crate) resolve_import_to_known_path` (any-target resolver).
  - Added `pub fn build_reverse_adjacency(files, internal_names,
    suffix_index) -> HashMap<String, Vec<ReverseEdge>>`. Single O(N×D)
    pass; per-file deduping merges multiple imports of the same target
    into a single `ReverseEdge` and tracks the smallest `line`.
    Self-edges skipped at edge-build time.
  - Added `pub fn compute_transitive_dependents(target, reverse, depth)
    -> TransitiveResult`. Cycle-safe BFS via `HashSet<String>` keyed by
    file path; deterministic via lex-sorted parents and edges (diamond
    tie-break is stable); direct entries enumerated before transitive
    expansion so they survive `MAX_DEPENDENTS` capping; `import_names`
    and `line` populated only at depth 1 (transitive entries do not
    carry IR-level import metadata).
- Files changed: `crates/seshat-graph/src/dependencies.rs` only.
- Quality: `cargo test -p seshat-graph` → 237 tests pass; the 5 required
  BFS tests all green
  (`transitive_depth_2_includes_2nd_order`,
  `transitive_depth_3_includes_3rd_order`,
  `transitive_cycle_a_b_a_terminates`,
  `transitive_diamond_visits_each_node_once`,
  `transitive_truncation_caps_at_max_dependents`).
  `cargo clippy --workspace --all-targets -- -D warnings` clean.
  `cargo check --workspace` clean.
- **Learnings for future iterations:**
  - Pre-existing `import_resolves_to_target` already had inline package
    boundary logic. Factoring it into `package_boundary_ok` was the
    cleanest way to satisfy AC #6 ("extract shared helper") without
    breaking the 5 cross-crate guard tests at lines 1695..1840.
  - `resolve_import` (forward resolver) does NOT have the cross-crate
    guard. The new `resolve_import_to_known_path` wraps it and applies
    the guard, so the reverse-adjacency builder agrees with the forward
    `import_resolves_to_target` view exactly.
  - `DependentEntry` is constructed in exactly one place
    (`build_dependents` in dependencies.rs). Future stories that touch
    this struct need only update that site + tests — no other crate
    constructs it.
  - For BFS via tracking, store the *chain to here* (intermediates
    inclusive of self) in each frontier node — the parent's chain is
    the child's `via`, and the child's chain is parent.chain + self.
    This avoids re-deriving via from scratch at each level.
  - `cargo test -p seshat-graph` cold-build is ~30s; subsequent runs
    are ~6s. `cargo clippy --workspace --all-targets` is ~5s incremental
    once dependencies are warm.
---

## 2026-05-10 12:50 (EEST) - US-002
- Added depth parameter to graph-layer query_dependencies API in
  `crates/seshat-graph/src/dependencies.rs`:
  - `pub const MAX_TRANSITIVE_DEPTH: u32 = 10`.
  - `pub struct QueryDependenciesOptions { pub depth: u32 }` +
    `impl Default { depth: 1 }`. `Copy` so callers can pass it freely.
  - `DependencyData` gained `transitive_dependent_count: usize` and
    `requested_depth: u32` (both `#[serde(default)]` to keep envelope
    forward-compat — DependencyData is `Serialize`-only so no helper
    function is needed).
  - `query_dependencies(conn, branch_id, target_path, opts)` and
    `query_dependencies_batch(conn, branch_id, paths, opts)` gained
    a trailing `opts` parameter.
  - For `depth == 1`, both keep the existing `build_dependents`
    fast path — no semantic regression (the 237 seshat-graph tests
    plus the validate_approach/diff_impact callsites prove this).
  - For `depth >= 2`, `build_reverse_adjacency` is called and
    `compute_transitive_dependents` does the BFS. In `_batch`, the
    reverse adjacency is built **exactly once** across the full IR
    and shared across every target.
  - `blast_radius` is classified from the **direct** dependent count
    only (computed by filtering `entries.iter().filter(|e| e.depth == 1)`)
    — existing thresholds preserved across opt-in transitive queries.
  - New private `validate_depth()` rejects `depth == 0` or
    `depth > MAX_TRANSITIVE_DEPTH` with `GraphError::InvalidInput`
    before any IR load (cheap fail-fast).
- Exports: `crates/seshat-graph/src/lib.rs` re-exports
  `QueryDependenciesOptions` and `MAX_TRANSITIVE_DEPTH` at the crate
  root so MCP-layer callers can use `seshat_graph::QueryDependenciesOptions`.
- Callers updated:
  - `crates/seshat-graph/src/validate_approach.rs:546` — passes
    `QueryDependenciesOptions::default()` (preserves direct-only
    semantics for `enrich_used_by`'s `dup.used_by` count).
  - `crates/seshat-graph/src/diff_impact.rs:378` — passes
    `QueryDependenciesOptions::default()` (preserves the current
    `dependent_count` / `blast_radius` semantics in `map_diff_impact`;
    US-009 will switch this site to `depth: DEFAULT_TRANSITIVE_DEPTH`).
  - `crates/seshat-mcp/src/tools/query_dependencies.rs:113` — passes
    `seshat_graph::QueryDependenciesOptions::default()` (the MCP tool
    still hard-wires `depth=1` until US-003 wires it up).
- Files changed: `crates/seshat-graph/src/dependencies.rs`,
  `crates/seshat-graph/src/lib.rs`,
  `crates/seshat-graph/src/validate_approach.rs`,
  `crates/seshat-graph/src/diff_impact.rs`,
  `crates/seshat-mcp/src/tools/query_dependencies.rs`.
- Quality: `cargo check --workspace` clean, `cargo test --workspace`
  all pass (237 in seshat-graph, full suite green; one unrelated
  serve_guardrails test was flaky under workspace concurrency but
  passes when run in isolation), `cargo clippy --workspace
  --all-targets -- -D warnings` clean.
- **Learnings for future iterations:**
  - `DependencyData` derives `Serialize` only (no `Deserialize`).
    Using `#[serde(default = "fn_name")]` on new fields would emit a
    `dead_code` warning because the default function is never called
    by serialize. Plain `#[serde(default)]` is safe and matches the
    existing pattern used by `truncated`/`depth`/`via`.
  - The `direct_count` for blast-radius classification at `depth >= 2`
    must be derived from the BFS result by filtering on
    `entry.depth == 1`. Don't try to call `build_dependents` separately
    — that re-walks the full IR and discards the amortisation benefit
    of the reverse-adjacency map.
  - `query_dependencies_batch` with `depth > 1` should build the
    reverse adjacency **once** and reuse it across targets. Each
    target's `compute_transitive_dependents(target, &reverse, depth)`
    is then O(BFS) instead of O(IR + BFS).
  - `MAX_TRANSITIVE_DEPTH = 10` was chosen as the upper bound. The
    cap matters because BFS at unbounded depth on a dense graph could
    push past `MAX_DEPENDENTS = 500` for almost every node.
  - When updating many test-callsite signatures, a single `perl -i -pe`
    run across the file is faster than dozens of `Edit` calls. The
    pattern is uniform (`query_dependencies(&conn, "main", "...")`)
    so a regex captures it cleanly without affecting `_batch`.
---
