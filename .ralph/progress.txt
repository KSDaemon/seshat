# Ralphy Progress Log
Started: понедельник, 30 марта 2026 г. 00:42:20 (EEST)
---

## Codebase Patterns
- seshat-core re-exports all public types from lib.rs — use `seshat_core::TypeName` not `seshat_core::module::TypeName`
- Every crate has its own `error.rs` with `thiserror` derive macros
- Detectors only depend on `seshat-core` (for IR types) plus `rayon`, `tracing`, `thiserror` — no storage/scanner deps
- `ConventionFinding`, `DetectorResults`, `CodeEvidence` live in `seshat-core::detector_result` (re-exported from lib.rs)
- `DetectionConfig` thresholds: >0.85 Strong, 0.50-0.85 Moderate, 0.20-0.50 Weak, <0.20 Info (boundary values go to the lower tier)
- `Language::all()` returns all 4 supported languages as a static slice
- `LanguageIR` enum variants: `Rust(RustIR)`, `TypeScript(TypeScriptIR)`, `JavaScript(JavaScriptIR)`, `Python(PythonIR)`
- Test pattern: inline `#[cfg(test)] mod tests` at bottom of each file; use `make_project_file()` from test_helpers for quick ProjectFile construction
- Pipeline design (ADR-6): files processed in parallel via `rayon::par_iter()`, detectors run sequentially per file
- Failing detectors are caught via `std::panic::catch_unwind` and logged with `tracing::warn` — never crash the pipeline
- `#[tracing::instrument(skip_all)]` on public pipeline functions
- Workspace uses Rust edition 2024 (min 1.85)
- Detector pattern: create struct implementing `ConventionDetector`, register in `all_detectors()` in pipeline.rs, add `pub mod` to lib.rs
- `DependencyUsage` has `package`, `import_path`, `line` — `package` is the crate/package name, `import_path` is the full import path
- Domain classification: use match on package name per language to map to `DependencyDomain` enum
- Convention findings: `follows_convention: true` for canonical library usage, `false` for conflicts and dead deps
- Use `KnowledgeNature::Convention` for canonical library findings, `KnowledgeNature::Observation` for conflicts and dead deps
- Import classification: Rust (`std/core/alloc` → Stdlib, `crate/super/self` → Internal, rest → External), TS/JS (relative `./`/`../` → Internal, node builtins → Stdlib, rest → External), Python (known stdlib list → Stdlib, relative `.` or `src/app/lib` prefix → Internal, rest → External)
- `Import` struct: `module` (path), `names` (imported symbols), `is_type_only` (TS type-only), `line` (source line)
- For grouping analysis, check monotonic non-decreasing order of ImportGroup enum values and line gaps >1 for blank-line separation
- `RustIR.error_types` populated by scanner for structs/enums with "Error" in name — use directly, no re-parsing needed
- `RustIR.derive_macros` has `DeriveUsage { type_name, derives, line }` — check `derives` for specific derive macro names
- `RustIR.trait_implementations` has `TraitImpl { trait_name, type_name, line }` — useful for detecting trait impls like `Error`, `From<T>`
- Use `DETECTOR_NAME` constant for detector name to keep it consistent across findings
- Evidence helper functions should accept `&[&T]` (not `&[T]`) to avoid forcing callers to clone filtered references
- Doc comments referencing types from other crates: use backtick-quoted `` `TypeName` `` not `[TypeName]` (rustdoc can't resolve cross-crate types without re-import)
- Self-referencing trait methods in doc comments: `[method](Self::method)` syntax
- Use `std::sync::LazyLock` for module-level static `HashSet`/`HashMap` initialization (stable in edition 2024)
- Never use `.expect()` or `.unwrap()` in library code (non-test) — use `let-else`, `if let`, or `?` instead
---

## 2026-03-30 01:00 (EEST) - US-001
- What was implemented:
  - `ConventionDetector` trait in `trait_def.rs` with `name()`, `detect()`, `supported_languages()` methods (Send + Sync for rayon)
  - `confidence.rs` with `compute_confidence()`, `weight_from_confidence()`, `aggregate_findings()`, and `AggregatedConvention` type
  - `pipeline.rs` with `run_all_detectors()`, `run_detectors()`, `all_detectors()`, and internal `run_detectors_on_file()`
  - Updated `lib.rs` to export all new modules and key types
  - Added `rayon` to workspace and seshat-detectors dependencies
- Files changed:
  - `Cargo.toml` (workspace — added rayon)
  - `crates/seshat-detectors/Cargo.toml` (added rayon dep)
  - `crates/seshat-detectors/src/lib.rs` (added module declarations and re-exports)
  - `crates/seshat-detectors/src/trait_def.rs` (NEW)
  - `crates/seshat-detectors/src/confidence.rs` (NEW)
  - `crates/seshat-detectors/src/pipeline.rs` (NEW)
- **Learnings for future iterations:**
  - `ConventionDetector` trait must be `Send + Sync` because rayon processes files in parallel and holds references to detectors across threads
  - `std::panic::catch_unwind(AssertUnwindSafe(...))` is the pattern for catching panicking detectors without crashing the pipeline
  - `run_detectors()` is the testable lower-level function (accepts custom detector list); `run_all_detectors()` is the convenience wrapper using `all_detectors()`
  - Boundary confidence values (exactly 0.85, 0.50, 0.20) map to the LOWER tier (e.g., 0.85 -> Moderate, not Strong)
  - `aggregate_findings` groups by (detector_name, description) tuple — both must match for findings to be aggregated together
  - Evidence collection in aggregation is bounded by `config.max_snippet_lines` to prevent unbounded memory growth
---

## 2026-03-30 02:30 (EEST) - US-002
- What was implemented:
  - `dependency_usage.rs` detector implementing `ConventionDetector` for all 4 languages
  - `DependencyDomain` enum with 8 domains: HTTP, logging, testing, validation, serialization, database, CLI, async runtime
  - Curated domain classification mappings for Rust, TypeScript/JavaScript, and Python packages
  - Canonical library detection: most-used library per domain identified by import count
  - Conflicting libraries flagged as `Observation` findings when multiple packages serve the same domain
  - Dead dependency detection: packages in `dependencies_used` without matching imports flagged
  - Each finding includes `CodeEvidence` with representative import paths
  - Registered `DependencyUsageDetector` in `all_detectors()` in pipeline.rs
  - 20 unit tests covering all detection scenarios, domain classification, and edge cases
  - Fixture files for Rust and TypeScript demonstrating dependency usage patterns
- Files changed:
  - `crates/seshat-detectors/src/dependency_usage.rs` (NEW)
  - `crates/seshat-detectors/src/lib.rs` (added module declaration)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, updated test)
  - `crates/seshat-detectors/tests/fixtures/rust_samples/dependency_usage.rs` (NEW fixture)
  - `crates/seshat-detectors/tests/fixtures/typescript_samples/dependency_usage.ts` (NEW fixture)
- **Learnings for future iterations:**
  - Detector pattern: create struct implementing `ConventionDetector`, register in `all_detectors()` in pipeline.rs, add `pub mod` to lib.rs
  - `DependencyUsage.package` is the crate/package name; `import_path` is the full qualified path
  - Domain classification uses simple match on package name — comprehensive but extensible
  - Dead dependency detection compares `dependencies_used` entries against `imports` by module name prefix matching
  - Use `KnowledgeNature::Convention` for canonical library, `KnowledgeNature::Observation` for conflicts/dead deps
  - When updating `all_detectors()` from empty to non-empty, must also update the test that asserted it was empty
  - TypeScript and JavaScript share the same domain classification (via `classify_js_ts`)
---

## 2026-03-30 04:15 (EEST) - US-003
- What was implemented:
  - `import_organization.rs` detector implementing `ConventionDetector` for all 4 languages
  - Import group classification: `ImportGroup` enum (Stdlib, External, Internal) with language-specific classifiers
  - Core grouping analysis: checks canonical ordering (Stdlib → External → Internal) and blank-line separation between groups
  - Rust-specific: `use` grouping statistics (std/core, external crates, crate/self/super counts)
  - TypeScript-specific: type-only import separation detection (grouped vs interleaved), barrel export file detection via `TypeScriptIR.has_barrel_exports`, barrel vs direct import preference
  - JavaScript-specific: `module.exports` detection alongside imports, barrel vs direct import preference
  - Python-specific: `import` vs `from`-import preference detection, isort-style grouping (stdlib → external → internal)
  - `follows_convention` correctly set: `true` for canonical ordering, `false` for non-canonical
  - Registered `ImportOrganizationDetector` in `all_detectors()` in pipeline.rs
  - 26 unit tests covering: classification per language, grouping detection, ordering, separation, language-specific patterns, edge cases
  - Existing fixture files already covered (import_grouping.rs, import_patterns.ts, import_grouping.py)
- Files changed:
  - `crates/seshat-detectors/src/import_organization.rs` (NEW — ~900 lines)
  - `crates/seshat-detectors/src/lib.rs` (added `pub mod import_organization`)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, updated all_detectors test)
- **Learnings for future iterations:**
  - Import classification is language-specific: Rust uses `::` separators, JS/TS uses path prefixes (`./`, `../`), Python uses dot-prefixed relative imports
  - Python stdlib detection requires a curated list of ~100+ top-level module names — non-exhaustive but covers common cases
  - Node.js builtins can use `node:` prefix or bare names (`fs`, `path`, `os`, etc.)
  - Grouping detection: check that ImportGroup enum values are monotonically non-decreasing in the import sequence (deduplicate consecutive equal groups first)
  - Blank-line separation: line gap >1 between imports from different groups indicates a blank-line separator
  - Type-only grouping: check that all imports between first and last type-only import are also type-only (contiguous block)
  - `TypeScriptIR.has_barrel_exports` is the authoritative flag for barrel export files — no need to re-derive from imports
  - Python `import X` → `names` is empty; `from X import Y` → `names` has entries — this distinguishes bare from from-imports
  - When creating test helpers that take language-specific IR, name them `make_*_file_with_ir()` to avoid unused function warnings
---

## 2026-03-30 06:00 (EEST) - US-004
- What was implemented:
  - `error_handling.rs` detector implementing `ConventionDetector` for all 4 languages
  - Rust: detects thiserror vs anyhow vs custom error enums via imports, `DeriveUsage`, and `RustIR.error_types`
  - Rust: detects `anyhow::Context` error wrapping pattern
  - Rust: detects manual `std::error::Error` trait implementations
  - Rust: detects `From<>` conversions for error types
  - Rust: flags thiserror + anyhow conflict as Observation
  - TypeScript: detects custom error classes (Class types with "Error" in name)
  - TypeScript: detects Result/Either pattern usage (neverthrow, fp-ts imports)
  - TypeScript: detects error type guard functions (is*Error pattern)
  - JavaScript: detects custom error classes and error handler functions
  - Python: detects custom exception hierarchy vs built-in exceptions
  - Python: detects contextlib usage for error context management
  - Python: detects error handling utility functions
  - Registered `ErrorHandlingDetector` in `all_detectors()` in pipeline.rs
  - 25 unit tests covering all detection scenarios per language
- Files changed:
  - `crates/seshat-detectors/src/error_handling.rs` (NEW — ~600 lines)
  - `crates/seshat-detectors/src/lib.rs` (added `pub mod error_handling`)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, updated all_detectors test)
- **Learnings for future iterations:**
  - `RustIR.error_types` is populated by the scanner for any struct/enum whose name contains "Error" — use this to detect error types without re-parsing
  - `RustIR.derive_macros` contains `DeriveUsage` with `type_name` and `derives` vec — check for `"Error"` in derives to detect thiserror usage
  - `RustIR.trait_implementations` contains `TraitImpl` with `trait_name` and `type_name` — check for `"std::error::Error"` or `"From<*>"` patterns
  - For TypeScript/JavaScript, error classes are detected via `TypeDef` with `kind: Class` and name containing "Error" — the IR doesn't have extends/superclass info
  - Python built-in exception names need a curated list; use `is_python_builtin_exception()` helper to distinguish custom from built-in
  - The `DETECTOR_NAME` constant pattern (used in error_handling.rs) keeps detector name consistent across all findings — consider adopting this in other detectors
  - Pre-commit hooks run `cargo fmt` which may reformat other files — use `git reset HEAD` + `git checkout` to undo unrelated formatting changes before committing
  - `TypeDefKind` does NOT implement `Copy` only `Clone` and `PartialEq` — use `==` comparison not pattern matching for kind checks
- Case pattern classification: strip leading/trailing underscores before classifying (Python dunder methods, private naming)
- Single-word names are ambiguous: treat as conforming to expected convention (e.g., single lowercase word matches snake_case, camelCase, kebab-case)
- For naming detectors, use `KnowledgeNature::Observation` for compiler-enforced languages (Rust), `KnowledgeNature::Convention` for community-driven (TS/JS/Python)
- Clippy `-D warnings` catches `type_complexity` for return types like `(Vec<(&str, usize)>, Vec<(&str, usize)>)` — extract a `type NameEntry<'a>` alias
---

## 2026-03-30 08:00 (EEST) - US-005
- What was implemented:
  - `naming.rs` detector implementing `ConventionDetector` for all 4 languages
  - `CasePattern` enum: SnakeCase, CamelCase, PascalCase, ScreamingSnakeCase, KebabCase, SingleLowerWord, SingleUpperWord, Unknown
  - `classify_case()` function: strips leading/trailing underscores, classifies names by character patterns
  - Function naming detection: analyses `ProjectFile.functions` names against expected convention per language
  - Type naming detection: analyses `ProjectFile.types` names (excluding constant-like names) against PascalCase
  - File naming detection: analyses file stem against expected convention (snake_case for Rust/Python, kebab-case for TS/JS)
  - Constant naming detection: identifies SCREAMING_SNAKE_CASE names from type definitions
  - Language-aware weighting: Rust findings use `Observation` (compiler-enforced), TS/JS/Python use `Convention`
  - Special file exclusion: mod.rs, lib.rs, main.rs, index.ts/js, __init__.py, etc.
  - `matches_expected()` handles ambiguous single-word names gracefully
  - Registered `NamingConventionsDetector` in `all_detectors()` in pipeline.rs
  - 30 unit tests covering: case classification, per-language function/type/file naming, mixed styles, constants, edge cases
- Files changed:
  - `crates/seshat-detectors/src/naming.rs` (NEW — ~580 lines)
  - `crates/seshat-detectors/src/lib.rs` (added `pub mod naming`)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, updated all_detectors test)
  - `.ralph/prd.json` (marked US-005 passes: true)
- **Learnings for future iterations:**
  - Case classification must handle edge cases: leading/trailing underscores (Python), single-word names, all-digit names
  - `is_likely_constant_name()` checks all-uppercase + underscores + digits to separate constants from type names
  - Special file names (mod.rs, index.ts, __init__.py) should be excluded from file naming analysis
  - Clippy `type_complexity` lint fires on tuple return types with nested generics — use type aliases
  - Adoption percentage: use `(conforming.len() as f64 / total as f64) * 100.0` and format with `{:.0}%`
  - Evidence building uses HashMap lookups to find the original `Function`/`TypeDef` for line numbers
   - `TypeDefKind` enum has: Struct, Enum, Trait, Interface, Class, TypeAlias — use `{:?}` for evidence snippets
- `Export` struct has `name`, `is_default`, `is_type_only`, `line` — booleans differentiate default/named and type-only exports
- `TypeScriptIR.has_barrel_exports` is the authoritative flag for barrel files; also check `is_barrel_file_path()` for `index.*`/`mod.*` heuristic
- `PythonIR.has_all_export` indicates `__all__` is defined; `PythonIR.is_init_file` indicates `__init__.py`
- `RustIR.mod_declarations` lists module names declared in the file — useful for detecting module root patterns in lib.rs/mod.rs
- `JavaScriptIR.has_module_exports` detects CommonJS `module.exports` pattern; `module_system` enum (Unknown, CommonJS, ESM)
- Clippy catches `redundant_closure` when passing a function reference — use `map(function_name)` not `map(|x| function_name(x))`
- Clippy catches `collapsible_if` for nested if statements — combine with `&&` when both conditions are simple
- For logging detection, classify packages into `LoggingLibrary` family (tracing/log/slog for Rust, winston/pino/bunyan for JS/TS, logging/loguru/structlog for Python) then merge evidence from both `dependencies_used` and `imports`
- Python stdlib packages (like `logging`) may only appear in `imports`, not `dependencies_used` — always check both sources
- Structured vs unstructured logging: inherently structured libs (tracing, slog, pino, bunyan, structlog) → structured; `log` crate, stdlib logging → unstructured; configurable libs (winston, log4js) → default unstructured
---

## 2026-03-30 10:00 (EEST) - US-006
- What was implemented:
  - `export_patterns.rs` detector implementing `ConventionDetector` for all 4 languages
  - TypeScript: default vs named export preference with adoption counts; barrel export detection via `TypeScriptIR.has_barrel_exports` and path heuristic; type-only export detection
  - JavaScript: default vs named export preference; barrel export detection via index file path; CommonJS `module.exports` pattern detection
  - Rust: pub visibility pattern analysis (pub items / total items percentage); mod declaration detection (Convention for lib.rs/mod.rs, Observation for others); pub use re-export detection
  - Python: `__all__` explicit public API detection; `__init__.py` package re-export point detection; missing `__all__` flagged as Observation
  - Helper functions: `export_evidence()`, `partition_exports()`, `is_barrel_file_path()`, `is_lib_or_mod_file()`
  - Registered `ExportPatternsDetector` in `all_detectors()` in pipeline.rs
  - 27 unit tests covering: trait basics, TS (named/default/mixed/barrel-IR/barrel-path/type-only/empty), JS (named/default/mixed/barrel/commonjs/empty), Rust (pub-visibility/mod-lib/mod-regular/reexports/no-public), Python (__all__/init-reexport/no-__all__/empty/init+all), cross-language dispatch, evidence capping
- Files changed:
  - `crates/seshat-detectors/src/export_patterns.rs` (NEW — ~530 lines)
  - `crates/seshat-detectors/src/lib.rs` (added `pub mod export_patterns`)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, added import, updated all_detectors test)
  - `.ralph/prd.json` (marked US-006 passes: true)
- **Learnings for future iterations:**
  - `Export.is_default` and `Export.is_type_only` are booleans — partition using simple filter predicates
  - Barrel export detection uses both IR flag (`has_barrel_exports`) and path heuristic (`index.*` stem) for coverage
  - Rust pub visibility analysis combines functions and types to compute public item percentage
  - Python missing `__all__` is an Observation (not Convention) — it's a recommendation, not a detected pattern
  - Clippy `redundant_closure` lint: use `map(fn_name)` instead of `map(|x| fn_name(x))` for function references
  - Clippy `collapsible_if` lint: combine nested ifs with `&&` when both conditions are straightforward
  - Evidence capping at 5 entries prevents unbounded output — apply `.take(5)` consistently
---

## 2026-03-30 12:00 (EEST) - US-007
- What was implemented:
  - `logging_observability.rs` detector implementing `ConventionDetector` for all 4 languages
  - `LoggingLibrary` enum classifying packages into families: Tracing/Log/Slog (Rust), Winston/Pino/Bunyan/Log4js (JS/TS), StdlibLogging/Loguru/Structlog (Python)
  - Per-language classification functions (`classify_rust_logging`, `classify_js_ts_logging`, `classify_python_logging`)
  - Canonical logging library identification: most evidence = primary library
  - Conflicting logging libraries in same file flagged as `Observation` findings
  - Structured vs unstructured logging style detection per language
  - Rust: tracing → structured (especially with instrument import), log → unstructured
  - JS/TS: pino/bunyan → structured, winston/log4js → unstructured
  - Python: structlog → structured, stdlib logging → unstructured, loguru → unstructured (by default)
  - Evidence collection from both `DependencyUsage` and `Import` entries, merged and capped at 5
  - Python-specific: also checks `imports` for stdlib `logging` (not in `dependencies_used`)
  - Registered `LoggingObservabilityDetector` in `all_detectors()` in pipeline.rs
  - 25 unit tests covering: trait basics, Rust (tracing/log/slog/conflicts/structured/unstructured/tracing-subscriber), TS (winston/pino/bunyan/pino-http), JS (winston/conflicts), Python (stdlib/loguru/structlog/conflicts/deduplication), evidence capping, no-logging edge case, classification coverage
- Files changed:
  - `crates/seshat-detectors/src/logging_observability.rs` (NEW — ~550 lines)
  - `crates/seshat-detectors/src/lib.rs` (added `pub mod logging_observability`)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, added import, updated all_detectors test)
  - `.ralph/prd.json` (marked US-007 passes: true)
- **Learnings for future iterations:**
  - Python stdlib packages (`logging`) appear only in `imports`, not `dependencies_used` — merge both sources when detecting libraries
  - `LoggingLibrary::is_structured()` maps inherently structured libraries (tracing, slog, pino, bunyan, structlog) — configurable libs default to unstructured
  - Evidence merging pattern: collect from `dependency_evidence()` and `import_evidence()` separately, then `merge_evidence()` to combine per-library with cap
  - Don't add enum variants that are never constructed (e.g., `ConsoleLog` for console.log detection via function names) — clippy catches dead code
- For tracing structured detection, check `imports` for `instrument` name rather than `derive_macros` (instrument is a proc-macro attribute, not a derive)
- The per-language `detect_*` functions follow the same pattern: collect evidence → find primary library → report canonical → flag conflicts → detect structured/unstructured
- Test detection: `ProjectFile` has no test-specific fields — infer from path patterns, imports, function names, and language IR (e.g., `PythonIR.decorators` for `@pytest.fixture`)
- Test file placement: path-based heuristics per language (e.g., `tests/` → Separate, `*.test.ts` → CoLocated, `test_*.py` → CoLocated)
- Rust inline tests detected via `RustIR.mod_declarations` containing "tests" and/or functions starting with `test_`
- JS/TS framework detection: check `dependencies_used` and `imports` for known packages, fall back to function name inference (`describe`/`it`/`test` → Jest by default)
---

## 2026-03-30 14:00 (EEST) - US-008
- What was implemented:
  - `test_patterns.rs` detector implementing `ConventionDetector` for all 4 languages
  - `TestFramework` enum: RustBuiltin, Jest, Vitest, Mocha, Pytest, Unittest
  - `TestPlacement` enum: CoLocated, Separate — path-based heuristics per language
  - `TestNamingStyle` enum: TestPrefix, DescribeIt, TestFunction, TestClass
  - `SetupPattern` enum: Hooks, PytestFixtures, SetUpTearDown, TestBuilder
  - Rust: detects built-in `#[test]` framework, inline test modules via `RustIR.mod_declarations`, `test_*` naming, builder/factory setup patterns
  - TypeScript/JavaScript: detects Jest/Vitest/Mocha via imports and deps, co-located (`.test.`/`.spec.`) vs separate (`__tests__/`, `tests/`), describe/it and test() naming, beforeEach/afterEach hooks
  - Python: detects pytest/unittest via imports, `test_*` function naming, `TestClass` classes, `@pytest.fixture` decorators, `setUp/tearDown` methods, `@pytest.mark.parametrize` and markers
  - JS/TS framework inference from globals (describe/it/test → Jest default) when no explicit imports
  - Registered `TestPatternsDetector` in `all_detectors()` in pipeline.rs
  - 35 unit tests covering: trait basics, Rust (framework/inline/separate/naming/builder), TS (jest-import/jest-dep/vitest/mocha/colocated/separate/spec/describe-it/hooks/globals), JS (jest/separate), Python (pytest/unittest/separate/colocated/naming/classes/fixtures/setup-teardown/parametrize/markers), cross-language (non-test/evidence-capping), framework classification, placement detection
- Files changed:
  - `crates/seshat-detectors/src/test_patterns.rs` (NEW — ~780 lines)
  - `crates/seshat-detectors/src/lib.rs` (added `pub mod test_patterns`)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, added import, updated all_detectors test)
  - `.ralph/prd.json` (marked US-008 passes: true)
- **Learnings for future iterations:**
  - `ProjectFile` has no test-specific fields — all test detection must be inferred from path, imports, function names, type names, and language-specific IR
  - `RustIR.mod_declarations` containing "tests" is the signal for inline `#[cfg(test)] mod tests` — no need to parse attributes
  - `PythonIR.decorators` contains full decorator strings like `"pytest.fixture"`, `"pytest.mark.parametrize"`, `"pytest.mark.slow"` — use `.starts_with()` for matching
  - JS/TS test frameworks can be detected from globals when no explicit imports exist — common for Jest which provides `describe`, `it`, `test`, `expect` globally
  - Test file placement is purely path-based per language; normalize path separators (`\` → `/`) before matching
  - Python test classes are detected via `TypeDef` with name starting with "Test" — no need for the IR to have explicit test class markers
  - Setup/teardown patterns differ significantly by language: hooks (JS/TS), fixtures (pytest), setUp/tearDown (unittest), builder functions (Rust)
- File structure detection: per-file path classification with pipeline aggregation — no interior mutability or trait changes needed
- When both by-type and by-layer dirs appear in a path, pick the earliest (highest-level) match — it's the primary organizer
- For common directory detection, report the deepest (most specific) match, not the shallowest
---

## 2026-03-30 16:00 (EEST) - US-009
- What was implemented:
  - `file_structure.rs` detector implementing `ConventionDetector` for all 4 languages
  - Directory organization pattern detection: by-type (models/, services/, controllers/), by-layer (domain/, infrastructure/, application/), and by-feature (domain-specific directories like users/, orders/)
  - When both by-type and by-layer dirs appear in a path, earliest (highest-level) match wins — correctly handles `src/domain/entities/` as by-layer
  - Common directory convention detection (src/, lib/, tests/, utils/, etc.) — reports deepest (most specific) match
  - Configuration file placement detection: root-level vs config-directory, with curated patterns for all languages (tsconfig, Cargo.toml, pyproject.toml, eslint, docker, etc.)
  - `is_potential_feature_dir()` excludes hidden dirs, single-char dirs, and all well-known directory names
  - Mutual exclusivity: by-type/by-layer wins over by-feature; a file votes for exactly one org pattern
  - Design decision documented in module doc comment: per-file classification with pipeline aggregation, no trait changes needed
  - Registered `FileStructureDetector` in `all_detectors()` in pipeline.rs
  - 40 unit tests covering: trait basics, by-type (5 dir types), by-layer (4 dir types), by-feature (3 scenarios), mutual exclusivity, common directories (4 types), config placement (root/config-dir/deeply-nested), cross-language, path components, edge cases (deep nesting, hidden dirs, crates workspace)
- Files changed:
  - `crates/seshat-detectors/src/file_structure.rs` (NEW — ~940 lines)
  - `crates/seshat-detectors/src/lib.rs` (added `pub mod file_structure`)
  - `crates/seshat-detectors/src/pipeline.rs` (registered detector, added import, updated all_detectors test)
  - `.ralph/prd.json` (marked US-009 passes: true)
- **Learnings for future iterations:**
  - The ConventionDetector per-file design can handle project-wide analysis via per-file classification + aggregation — each file "votes" for the pattern it exhibits
  - When multiple well-known directory types overlap in a path (e.g., `domain/entities`), use position-based priority (earliest = primary organizer)
  - `find_matching_component_with_index()` returns `(usize, &str)` to enable position comparison between type and layer matches
  - For common directory detection, deepest match is more informative (e.g., `utils` > `src` for `src/utils/helpers.ts`)
  - Configuration file detection uses both file name patterns and directory placement (root vs config/ dir)
  - `path_components()` strips the file name and returns only directory segments — essential for path-based analysis
  - Feature directories are identified as directories after a source root (`src/`, `lib/`, `app/`, `crates/`, `packages/`) that don't match any well-known category
- Cross-reference module lives in seshat-graph (per ADR-23) as a pure in-memory function — accepts `&[KnowledgeNode]` slices, returns `CrossReferenceResult` with edges and boosted nodes. Caller (graph layer) loads from storage and persists results.
- Keyword matching: `extract_keywords()` removes stop words, `jaccard_similarity()` computes overlap. Threshold 0.15 for topical relatedness.
- Choice extraction: look for words after signal phrases ("use ", "prefer ", "canonical: ", etc.) to find concrete library/tool names
- Contradiction vs reinforcement: if choices overlap → reinforcement (confidence boost), if choices differ → contradiction (Contradicts edge)
- `serde_json` must be added to seshat-graph Cargo.toml if creating JSON metadata for edges
---

## 2026-03-30 18:00 (EEST) - US-010
- What was implemented:
  - `cross_reference.rs` in seshat-graph implementing cross-reference of code conventions vs documentation (ADR-23)
  - Pure in-memory function: `cross_reference(code_conventions, doc_nodes, branch_id, config) -> CrossReferenceResult`
  - Keyword extraction: lowercases, splits on non-alphanumeric, removes stop words, filters short tokens
  - Jaccard similarity for topical matching between doc and convention descriptions
  - Choice extraction: signal phrase detection ("use ", "prefer ", "canonical: ", etc.) to find concrete library/tool names
  - Relationship classification: overlapping choices → reinforcement, different choices → contradiction, no choices → unrelated
  - Contradiction: creates `Edge` with `EdgeType::Contradicts`, weight = keyword overlap, metadata with descriptions and reason
  - Reinforcement: asymptotic confidence boost `new = old + boost * (1.0 - old)`, returns `ReinforcedNode` with original/boosted values
  - `CrossReferenceConfig` with configurable `min_keyword_overlap` (default 0.15) and `reinforcement_boost` (default 0.10)
  - `CrossReferenceResult` with `contradiction_edges` and `reinforced_nodes`
  - Exported from `seshat-graph/src/lib.rs`: `cross_reference`, `CrossReferenceConfig`, `CrossReferenceResult`, `ReinforcedNode`
  - Added `serde_json` dependency to seshat-graph for edge metadata
  - 39 unit tests covering: contradictions (3 tests), reinforcements (3 tests), no-match/empty (5 tests), multiple nodes (1 test), edge properties (2 tests), keyword extraction (5 tests), Jaccard similarity (4 tests), choice extraction (5 tests), relationship classification (3 tests), confidence boosting (4 tests), weight mapping (1 test), config (2 tests)
- Files changed:
  - `crates/seshat-graph/src/cross_reference.rs` (NEW — ~960 lines)
  - `crates/seshat-graph/src/lib.rs` (added `pub mod cross_reference` and re-exports)
  - `crates/seshat-graph/Cargo.toml` (added `serde_json` dependency)
  - `.ralph/prd.json` (marked US-010 passes: true)
- **Learnings for future iterations:**
  - seshat-graph already depends on seshat-storage, but cross_reference is implemented as pure in-memory function for testability — no storage mocks needed
  - `EdgeType::Contradicts` already existed in seshat-core — no need to add new edge types
  - There is no `Reinforces` edge type; reinforcement is modeled as confidence boost on the node itself
  - `NodeRepository::find_by_nature()` is global (not branch-scoped) — filter client-side by `branch_id` if needed
  - Keyword overlap threshold of 0.15 (Jaccard) works well for matching descriptions about the same domain
  - Test descriptions need enough shared keywords to meet the threshold — use domain-specific terms in both doc and convention descriptions
  - Clippy catches `doc_lazy_continuation` when `>` in doc comments is interpreted as blockquote — use backtick-quoted `>0.85` instead
  - `serde_json::json!()` macro requires `serde_json` as a direct dependency in Cargo.toml, even if transitively available
---

## 2026-03-30 20:00 (EEST) - US-011
- What was implemented:
  - Branch code review and quality gate — comprehensive review of all changes in feat/convention-detection-engine
  - All 4 automated quality gates verified passing: `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --workspace` (all 260 detector + 39 graph + other tests), `cargo doc --no-deps --document-private-items`
  - Fixed 8 broken intra-doc links across seshat-detectors, seshat-storage, seshat-scanner
  - Replaced `.expect()` with `let-else` in `dependency_usage.rs` — library code must not panic
  - Refactored evidence helper functions in `test_patterns.rs` from `&[T]` to `&[&T]` — eliminated 8 unnecessary `.clone()` + `.collect()` allocations
  - Added `LazyLock` for stop-word `HashSet` in `cross_reference.rs` — prevents rebuilding per-call in hot loop
  - Added doc comments to all 4 undocumented public fields on `ReinforcedNode`
  - Applied `cargo fmt --all` to fix formatting across all crates
  - Full diff review confirmed: no unsafe blocks, no unwrap/expect in library code, no crate boundary violations, confidence scoring centralized, correct dependency boundaries
- Files changed:
  - `crates/seshat-detectors/src/dependency_usage.rs` (fixed .expect() → let-else, fixed doc link)
  - `crates/seshat-detectors/src/error_handling.rs` (fixed doc link)
  - `crates/seshat-detectors/src/export_patterns.rs` (fixed 2 doc links)
  - `crates/seshat-detectors/src/logging_observability.rs` (fixed doc link)
  - `crates/seshat-detectors/src/pipeline.rs` (fixed doc link)
  - `crates/seshat-detectors/src/test_patterns.rs` (refactored evidence helpers to &[&T], eliminated 8 clones)
  - `crates/seshat-graph/src/cross_reference.rs` (LazyLock for stop words, doc comments on ReinforcedNode)
  - `crates/seshat-storage/src/repository/mod.rs` (fixed doc link)
  - `crates/seshat-scanner/src/parser/mod.rs` (fixed doc link)
  - Multiple files reformatted by `cargo fmt`
- **Learnings for future iterations:**
  - `cargo doc` warnings for broken intra-doc links: types from other crates (e.g., `RustIR`, `PythonIR`) aren't in scope — use backtick-quoted `TypeName` instead of `[TypeName]`
  - Self-referencing trait methods in doc comments: use `[method](Self::method)` syntax
  - Evidence helper functions should accept `&[&T]` to avoid forcing callers to clone owned data from filter iterators
  - `std::sync::LazyLock` (stable in edition 2024) is the standard way to initialize static sets/maps once
  - `cargo fmt` may reformat files outside your direct changes — always run fmt before committing and stage everything
  - The full review found no major architectural issues: crate boundaries are clean, confidence scoring is centralized, no copy-paste of scoring logic across detectors
---
