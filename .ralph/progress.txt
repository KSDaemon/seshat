# Ralphy Progress Log
Started: вторник, 21 апреля 2026 г. 22:03:33 (EEST)
---

## Codebase Patterns
- `find_git_root` in `crates/seshat-cli/src/db.rs` handles worktree `.git` files by resolving `gitdir:` paths with normalization of `..` components
- `get_current_branch` in `crates/seshat-cli/src/db.rs` uses `gix::discover` + `read_head_file` fallback; returns commit hash (40-char hex) for detached HEAD
- Rust path normalization: `PathBuf::join` does NOT normalize `..` — must manually iterate components and handle `Component::ParentDir`
- Tests use `tempfile::tempdir()` for temp dirs; real git commands for git repo setup

## [2026-04-21 22:15 (EEST)] - US-001
- **What was implemented:** Completed US-001 — worktree detection in find_git_root and get_current_branch
  - Fixed path normalization bug in `find_git_root` for worktree `.git` files with relative `gitdir:` paths (was not handling `..` components)
  - Added detached HEAD commit hash support in `read_head_file` and `read_head_from_gitdir` (returns 40-char hex string instead of `None`)
  - Updated `get_current_branch_detached_head` test to assert `Some(commit_hash)` instead of `None`
- **Files changed:**
  - `crates/seshat-cli/src/db.rs` — `find_git_root` path normalization, `read_head_file` detached HEAD support, `read_head_from_gitdir` detached HEAD support, test fix
- **Learnings for future iterations:**
  - `PathBuf::join` does NOT normalize paths — `a/b/../c` stays as `a/b/../c`, not `a/c`
  - Manual component iteration needed for path normalization in tests with relative paths
  - Worktree `.git` file `gitdir:` relative paths are resolved from the `.git` file's parent directory
  - `gix::discover` handles worktrees correctly; the `gix` crate is already a dependency
- `scan_project` and `scan_project_with_progress` take a `branch_id: &str` parameter to scope scan data to a specific branch
- For serve.rs, detect branch early and clone before tokio::spawn to avoid borrow issues
- Banner shows both DB branch and detected branch for clarity: `{info.branch} (detected: {detected_branch})`
---

## [2026-04-21 22:45 (EEST)] - US-003
- **What was implemented:** Completed US-003 — branch snapshot on switch to new branch
  - `handle_branch_switch` function for ExistingDb path: compares detected branch with DB's current_branch, uses list_branches to check if target has data, creates snapshot from source if target has no data, calls switch_branch to update metadata
  - `handle_auto_scan_snapshot` function for AutoScan path: compares detected branch with "main", uses list_branches to check if "main" has data, creates snapshot from "main" to detected branch if main has data, switches to detected branch
  - Both paths wired into run_serve flow before submodule loading
- **Files changed:**
  - No new files — implementation was already complete in `serve.rs` (functions + wiring already present)
- **Learnings for future iterations:**
  - `handle_branch_switch` handles ExistingDb: source = DB's current_branch, target = detected branch
  - `handle_auto_scan_snapshot` handles AutoScan: source = "main", target = detected branch, only if "main" has data
  - For AutoScan fresh DB: "main" has no data → no snapshot needed, scan runs on detected branch directly
  - `switch_branch` updates `metadata` table with `current_branch` key via INSERT OR UPDATE
  - `list_branches` queries DISTINCT branch_id from nodes, edges, and files_ir tables
- `handle_branch_switch` and `handle_auto_scan_snapshot` are called before scan starts, ensuring branch is set before data is populated
---

## [2026-04-21 22:30 (EEST)] - US-002
- **What was implemented:** Completed US-002 — wire detected branch into serve flow
  - Added `detect_branch` function in `serve.rs` using `get_current_branch` with "main" fallback
  - Modified `scan_project` and `scan_project_with_progress` to accept `branch_id: &str` parameter (was hardcoded to "main")
  - Wired detected branch through `ProjectConnection::new` for both AutoScan and ExistingDb paths
  - Wired detected branch into `start_watcher` call
  - Wired detected branch into auto-scan background task
  - Updated banner to show both DB branch and detected branch
  - Updated all `scan_project`/`scan_project_with_progress` call sites in scanner tests, scan.rs tests, and watcher lib.rs
- **Files changed:**
  - `crates/seshat-scanner/src/orchestrator.rs` — `scan_project` + `scan_project_with_progress` accept `branch_id: &str`, updated all tests
  - `crates/seshat-cli/src/serve.rs` — `detect_branch` function, branch wiring through ProjectConnection/watcher/auto-scan, banner update
  - `crates/seshat-cli/src/scan.rs` — updated `scan_project_with_progress` calls to pass branch_id
  - `crates/seshat-watcher/src/lib.rs` — updated `scan_project` call to pass branch
  - `crates/seshat-scanner/tests/scan_orchestration_integration.rs` — updated all `scan_project` calls
  - `crates/seshat-scanner/tests/incremental_scan_integration.rs` — updated all `scan_project` calls
- **Learnings for future iterations:**
  - When cloning a String for tokio::spawn, clone BEFORE the spawn to avoid borrow checker issues
  - `scan_project_with_progress` callback must include branch_id as last argument (5th param)
  - `BranchId::from(detected_branch.as_str())` for converting String to BranchId
  - Submodule scans in scan.rs still use "main" — branch detection for scan command is a future enhancement
---
