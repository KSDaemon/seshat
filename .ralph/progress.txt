# Ralphy Progress Log
Started: четверг, 26 марта 2026 г. 18:09:49 (EET)
---

## Codebase Patterns
- All IR types (ProjectFile, Language, LanguageIR, etc.) live in seshat-core::ir — parsers import from seshat_core
- Use `LanguageIR::Rust(RustIR::default())` etc. for empty language-specific IR
- Test helpers behind `test-helpers` feature flag in seshat-core; use via `[dev-dependencies] seshat-core = { workspace = true, features = ["test-helpers"] }`
- Workspace dependencies declared in root Cargo.toml under `[workspace.dependencies]`, crates reference them with `.workspace = true`
- ScanError in seshat-scanner::error is the shared error type for the scanning pipeline
- Parser trait + dispatch live in seshat-scanner::parser::mod.rs; each language parser is a submodule
- Content hashing (SHA-256 via sha2 crate) is done in shared code (parser::content_hash), not per-parser
- Graceful degradation: parse_file returns empty ProjectFile with tracing::warn on parser error
- File discovery lives in seshat-scanner::discovery; uses `ignore` crate WalkBuilder for .gitignore support
- Language::from_extension(ext) detects language from file extension; returns None for unrecognised
- ScanConfig (in seshat-core::config) has exclude_patterns and max_file_size_kb — used by discover_files
- Use `tempfile` crate (workspace dep) for filesystem tests with temp directories
- Tree-sitter parsers: use `tree-sitter = "0.25"` and `tree-sitter-rust = "0.24"`, `tree-sitter-typescript = "0.23"` (compatible set)
- Tree-sitter parser setup: `TsParser::new()` → `set_language(&tree_sitter_rust::LANGUAGE.into())` → `parse(source, None)`
- Tree-sitter node traversal: iterate `root.child_count()` with `root.child(i)`, match on `child.kind()`
- Node text: `node.utf8_text(source.as_bytes())` — always pass `&[u8]` not `&str`
- Visibility detection: look for `visibility_modifier` child node on items
- Derive attributes: `attribute_item` → `attribute` child → text starts with "derive" → parse parens content
- Trait impls: `impl_item` with `for` keyword distinguishes trait impls from inherent impls
- Integration test fixtures go in `crates/seshat-scanner/tests/fixtures/<language>_project/`
- DeriveUsage and TraitImpl are now re-exported from seshat-core (added to lib.rs pub use)
- tree-sitter-typescript exposes LANGUAGE_TYPESCRIPT and LANGUAGE_TSX — use TSX for .tsx files
- TypeScript AST: imports → `import_statement` with `import_clause` (containing `named_imports`, `identifier`, or `namespace_import`)
- TypeScript AST: exports → `export_statement` with children: `export_clause`, `default`, `function_declaration`, `class_declaration`, `lexical_declaration`, `*`
- TypeScript AST: decorators on exported classes appear as children of `export_statement`, not `class_declaration`
- TypeScript AST: type-only imports/exports have a `type` child node (not an attribute like in Rust)
- TypeScript AST: `enum_declaration` uses `identifier` not `type_identifier` for its name (unlike interface/class/type_alias)
- Pre-commit hooks run cargo fmt — if hook fails, re-add formatted files and make a new commit
- tree-sitter-javascript 0.23 exposes `LANGUAGE` constant (single grammar handles both JS and JSX)
- JavaScript AST is similar to TypeScript: `import_statement`, `export_statement`, `function_declaration`, `class_declaration`, `lexical_declaration`, `expression_statement`
- CommonJS patterns: `require('module')` → `call_expression` with `identifier` "require" and `arguments` containing `string`
- Destructured require: `const { a, b } = require('module')` → `variable_declarator` with `object_pattern` + `call_expression`
- `object_pattern` children: `shorthand_property_identifier_pattern` (for `{ a }`) and `pair_pattern` (for `{ a: b }`)
- `module.exports = ...` and `exports.foo = ...` are `expression_statement` → `assignment_expression` → `member_expression`
- Module system detection order: check destructured require before plain require (destructured has `object_pattern` which would fail the simple `identifier` check)
- tree-sitter-python 0.23 is compatible with tree-sitter 0.25 — exposes a single `LANGUAGE` constant
- Python AST: `async def` is a `function_definition` with an `async` keyword child (kind="async"), NOT a separate node type
- Python AST: `import_statement` has `dotted_name` or `aliased_import` children; `import_from_statement` has `relative_import` for relative imports
- Python AST: `decorated_definition` wraps `decorator` + `function_definition`/`class_definition` — decorators are children, not siblings
- Python AST: `__all__` is `expression_statement` → `assignment` with `list` RHS containing `string` children
- Python string nodes have `string_content` child for extracting inner text (no quotes)
- Type hint detection: check `type` child (return annotation), `typed_parameter`/`typed_default_parameter` in params, AND class body methods/annotated assignments
- Manifest analysis lives in seshat-scanner::manifest; uses `toml` and `serde_json` crates (both workspace deps)
- Cargo.toml deps can be `dep = "version"` or `dep = { version = "...", ... }` or `dep.workspace = true` — use serde Deserialize with HashMap<String, toml::Value>
- PEP 508 dependency specs: name ends at first non-alphanumeric/non-dash/non-underscore/non-dot character
- Python package names normalise hyphens to underscores for import matching
- Cross-referencing: Rust/Python normalise hyphens→underscores; JS matches exact name or subpath (`dep/path`)
- ScanError::ManifestError variant for manifest parsing errors
- Module structure lives in seshat-scanner::module_structure; `build_module_graph(root, parsed_files, branch_id)` returns `ModuleGraph`
- ModuleGraph stores `path_to_node_id` and `node_id_to_path` maps for O(1) queryability via `dependencies_of()` / `dependents_of()`
- Modules are detected by directory: each directory containing source files = one module (Fact node)
- Import resolution uses `build_import_target_map` which registers files by path, stem, Rust `::` style, Python dotted style, and directory-level targets
- DependsOn edges are deduplicated per (source_module, target_module) pair using BTreeSet
- PartOf edges only created when parent directory is also a detected module (has source files)
- `parse_file` returns `ProjectFile` directly (not Result) — graceful degradation by design
---

## 2026-03-26 18:15 (EET) - US-001
- What was implemented: Parser trait, language dispatch (parse_file), SHA-256 content hashing, stub parsers for Rust/TypeScript/JavaScript/Python, graceful degradation on parse failure
- Files changed:
  - Cargo.toml (added sha2 workspace dep)
  - Cargo.lock (updated)
  - crates/seshat-scanner/Cargo.toml (added sha2 dep, dev-dependencies)
  - crates/seshat-scanner/src/lib.rs (added parser module + re-exports)
  - crates/seshat-scanner/src/parser/mod.rs (NEW: Parser trait, parse_file, content_hash, empty_project_file, tests)
  - crates/seshat-scanner/src/parser/rust_parser.rs (NEW: stub RustParser)
  - crates/seshat-scanner/src/parser/typescript_parser.rs (NEW: stub TypeScriptParser)
  - crates/seshat-scanner/src/parser/javascript_parser.rs (NEW: stub JavaScriptParser)
  - crates/seshat-scanner/src/parser/python_parser.rs (NEW: stub PythonParser)
- **Learnings for future iterations:**
  - The Parser trait returns Result<ProjectFile, ScanError>; content_hash field is overwritten by parse_file
  - Each language parser is in its own submodule file (rust_parser.rs, etc.) and imported in mod.rs
  - Stub parsers return empty ProjectFile with correct LanguageIR variant; tree-sitter parsing fills in actual data
  - All 87 workspace tests pass, clippy clean
---

## 2026-03-26 18:30 (EET) - US-002
- What was implemented: File discovery with .gitignore respect using `ignore` crate WalkBuilder, Language::from_extension() for extension-based language detection, DiscoveredFile type, discover_files() orchestration function
- Files changed:
  - Cargo.toml (added ignore, tempfile workspace deps)
  - Cargo.lock (updated)
  - crates/seshat-scanner/Cargo.toml (added ignore dep, tempfile dev-dep)
  - crates/seshat-scanner/src/lib.rs (added discovery module + re-exports)
  - crates/seshat-scanner/src/discovery.rs (NEW: DiscoveredFile, discover_files, 10 unit tests)
  - crates/seshat-core/src/ir.rs (added Language::from_extension, Language::all, tests)
- **Learnings for future iterations:**
  - WalkBuilder from `ignore` crate natively handles .gitignore, hidden files, .git/info/exclude
  - Custom exclude patterns use OverrideBuilder with negated globs (prefix `!`) to exclude matching files
  - WalkBuilder needs a .git directory present to activate .gitignore parsing — tests must create `.git/` dir
  - `filter_entry` callback on WalkBuilder lets you filter before descending into directories (used for .git exclusion)
  - tempfile::tempdir() creates a temp directory that is auto-cleaned on drop — good for filesystem tests
  - All 100 workspace tests pass, clippy clean
---

## 2026-03-26 19:00 (EET) - US-003
- What was implemented: Full tree-sitter-based Rust parser replacing the stub. Extracts imports (use declarations with groups, wildcards, aliases), functions (pub/private, async), types (structs, enums, traits, type aliases with visibility), exports (pub items), mod declarations, derive macros, trait implementations (impl Trait for Type), error types (names containing "Error"), and impl block methods.
- Files changed:
  - Cargo.toml (added tree-sitter, tree-sitter-rust workspace deps)
  - Cargo.lock (updated)
  - crates/seshat-scanner/Cargo.toml (added tree-sitter, tree-sitter-rust deps)
  - crates/seshat-scanner/src/parser/rust_parser.rs (REWRITTEN: full tree-sitter parser + 20 unit tests)
  - crates/seshat-core/src/lib.rs (added DeriveUsage, TraitImpl re-exports)
  - crates/seshat-scanner/tests/fixtures/rust_project/src/main.rs (NEW: fixture)
  - crates/seshat-scanner/tests/fixtures/rust_project/src/config.rs (NEW: fixture)
  - crates/seshat-scanner/tests/fixtures/rust_project/src/error.rs (NEW: fixture)
  - crates/seshat-scanner/tests/fixtures/rust_project/src/server.rs (NEW: fixture)
  - crates/seshat-scanner/tests/rust_parser_integration.rs (NEW: 22 integration tests)
- **Learnings for future iterations:**
  - tree-sitter is error-tolerant — even malformed source produces a parse tree (partial IR, no panic)
  - `scoped_use_list` node handles grouped imports like `use std::io::{Read, Write}`
  - `use_wildcard` handles glob imports like `use std::io::*`
  - Derive attributes appear as `attribute_item` siblings before the type they decorate — must track pending derives
  - `impl_item` children include type identifiers before/after the `for` keyword — order matters for trait vs type name
  - Comments and inner attributes should NOT clear pending derives (only actual items should)
  - All 143 workspace tests pass, clippy clean
---

## 2026-03-26 20:00 (EET) - US-004
- What was implemented: Full tree-sitter-based TypeScript parser replacing the stub. Extracts imports (named, default, namespace `* as`, type-only `import type`), exports (named, default, re-exports `export { } from`, barrel `export * from`, type-only `export type { }`), functions (declared, arrow, async), types (interfaces, type aliases, classes, abstract classes, enums), decorators (@Component, @Injectable), and TypeScript-specific IR (has_barrel_exports, type_only_imports, decorators, default_export). Handles both .ts and .tsx files using LANGUAGE_TYPESCRIPT and LANGUAGE_TSX grammars respectively.
- Files changed:
  - Cargo.toml (added tree-sitter-typescript workspace dep)
  - Cargo.lock (updated)
  - crates/seshat-scanner/Cargo.toml (added tree-sitter-typescript dep)
  - crates/seshat-scanner/src/parser/typescript_parser.rs (REWRITTEN: full tree-sitter parser + 34 unit tests)
  - crates/seshat-scanner/tests/fixtures/typescript_project/src/index.ts (NEW: barrel exports fixture)
  - crates/seshat-scanner/tests/fixtures/typescript_project/src/types.ts (NEW: types fixture)
  - crates/seshat-scanner/tests/fixtures/typescript_project/src/services.ts (NEW: classes/decorators fixture)
  - crates/seshat-scanner/tests/fixtures/typescript_project/src/app.tsx (NEW: TSX component fixture)
  - crates/seshat-scanner/tests/fixtures/typescript_project/src/utils.ts (NEW: utilities fixture)
  - crates/seshat-scanner/tests/typescript_parser_integration.rs (NEW: 30 integration tests)
- **Learnings for future iterations:**
  - tree-sitter-typescript 0.23 is compatible with tree-sitter 0.25
  - LANGUAGE_TYPESCRIPT vs LANGUAGE_TSX: use extension to pick — .tsx gets LANGUAGE_TSX to handle JSX
  - Decorators on exported classes are children of export_statement, not class_declaration — must extract from both levels
  - TypeScript import_clause has children: identifier (default import), named_imports (named), namespace_import (*)
  - export_specifier contains the full alias text (e.g., "default as App") — parse carefully
  - enum_declaration uses `identifier` child for name; interface/class/type_alias use `type_identifier`
  - Arrow functions and function expressions in `const` declarations need special handling via variable_declarator
  - All 206 workspace tests pass, clippy clean
---

## 2026-03-26 21:00 (EET) - US-005
- What was implemented: Full tree-sitter-based JavaScript parser replacing the stub. Handles both ESM and CommonJS module systems. ESM: extracts import statements (named, default, namespace), export statements (named, default, re-exports, barrel exports), functions, classes. CommonJS: extracts require() calls (simple and destructured), module.exports (object with named exports, single export, member exports), exports.foo patterns. Detects module system from file extension (.mjs→ESM, .cjs→CommonJS) and content heuristics. Populates JavaScriptIR with module_system, has_module_exports, and require_calls. Handles .js, .jsx, .mjs, .cjs file extensions.
- Files changed:
  - crates/seshat-scanner/src/parser/javascript_parser.rs (REWRITTEN: full tree-sitter parser + 39 unit tests)
  - crates/seshat-scanner/tests/fixtures/javascript_project/src/index.mjs (NEW: ESM entry point fixture)
  - crates/seshat-scanner/tests/fixtures/javascript_project/src/services.mjs (NEW: ESM classes/functions fixture)
  - crates/seshat-scanner/tests/fixtures/javascript_project/src/utils.mjs (NEW: ESM utilities fixture)
  - crates/seshat-scanner/tests/fixtures/javascript_project/src/config.cjs (NEW: CommonJS config fixture)
  - crates/seshat-scanner/tests/fixtures/javascript_project/src/middleware.js (NEW: CommonJS middleware fixture with exports.foo pattern)
  - crates/seshat-scanner/tests/fixtures/javascript_project/src/app.jsx (NEW: JSX component fixture)
  - crates/seshat-scanner/tests/javascript_parser_integration.rs (NEW: 34 integration tests)
  - .ralph/prd.json (US-005 passes: true)
- **Learnings for future iterations:**
  - tree-sitter-javascript 0.23 is compatible with tree-sitter 0.25 and exposes a single `LANGUAGE` constant (handles both JS and JSX)
  - Unlike tree-sitter-typescript which has separate LANGUAGE_TYPESCRIPT/LANGUAGE_TSX, JS uses one grammar for both
  - Destructured require (`const { a, b } = require('mod')`) uses `object_pattern` with `shorthand_property_identifier_pattern` children (NOT `shorthand_property_identifier` which is for object literals)
  - Must check for destructured require BEFORE simple require — both have `call_expression` child but destructured has `object_pattern` instead of `identifier`
  - `module.exports = { ... }` RHS object uses `shorthand_property_identifier` (not `_pattern`) for `{ foo }` shorthand properties
  - `exports.foo = ...` and `module.exports.foo = ...` both appear as `assignment_expression` with `member_expression` LHS
  - Module system detection: .mjs → ESM, .cjs → CommonJS, .js/.jsx → heuristic based on import/export vs require/module.exports
  - All 277 workspace tests pass, clippy clean
---

## 2026-03-26 22:00 (EET) - US-006
- What was implemented: Full tree-sitter-based Python parser replacing the stub. Extracts imports (`import x`, `from x import y`, relative imports `from . import`, `from .models import`, wildcard `from x import *`, aliased `import x as y`). Extracts `__all__` exports as structured Export entries. Extracts functions (sync and async via `async` keyword child detection), classes (simple and with inheritance). Detects type hints from function parameters (`typed_parameter`), return types (`type` child), and class body method annotations. Extracts decorators from `decorated_definition` nodes (simple `@decorator`, dotted `@app.route`, and callable `@retry(max_attempts=5)`). Populates PythonIR with `has_all_export`, `is_init_file` (detected from `__init__.py` filename), `type_hints_used`, and `decorators` list. Handles `.py` files.
- Files changed:
  - Cargo.toml (added tree-sitter-python workspace dep)
  - Cargo.lock (updated)
  - crates/seshat-scanner/Cargo.toml (added tree-sitter-python dep)
  - crates/seshat-scanner/src/parser/python_parser.rs (REWRITTEN: full tree-sitter parser + 31 unit tests)
  - crates/seshat-scanner/tests/fixtures/python_project/mypackage/__init__.py (NEW: package init fixture with __all__)
  - crates/seshat-scanner/tests/fixtures/python_project/mypackage/models.py (NEW: dataclass/enum fixture)
  - crates/seshat-scanner/tests/fixtures/python_project/mypackage/services.py (NEW: class inheritance fixture)
  - crates/seshat-scanner/tests/fixtures/python_project/mypackage/utils.py (NEW: utility functions fixture)
  - crates/seshat-scanner/tests/fixtures/python_project/mypackage/decorators.py (NEW: decorator patterns fixture)
  - crates/seshat-scanner/tests/fixtures/python_project/main.py (NEW: entry point fixture)
  - crates/seshat-scanner/tests/python_parser_integration.rs (NEW: 31 integration tests)
- **Learnings for future iterations:**
  - tree-sitter-python 0.23.6 is compatible with tree-sitter 0.25 and exposes `LANGUAGE` constant (single grammar)
  - `async def` in Python AST is NOT a separate node type — it's a `function_definition` with `async` keyword as a child node (kind="async")
  - Decorator nodes are children of `decorated_definition`, not siblings of the decorated item — extract from `decorator` children then the `function_definition`/`class_definition` child
  - `__all__ = [...]` is `expression_statement` → `assignment` → left=`__all__`, right=`list` with `string` children; use `string_content` child to extract without quotes
  - Type hints in class bodies require scanning the class `block` for `typed_parameter` in methods and annotated assignments
  - Clippy prefers `vec.append(&mut other_vec)` over `vec.extend(other_vec.drain(..))`
  - All 340 workspace tests pass, clippy clean
---

## 2026-03-26 23:00 (EET) - US-007
- What was implemented: Dependency manifest analysis module (`manifest.rs` in seshat-scanner). Parses Cargo.toml (TOML), package.json (JSON), and pyproject.toml (TOML/PEP 621) to extract declared dependencies with version info and dev/non-dev classification. Cross-references declared dependencies against parsed IR imports to count files using each dependency and flags dead (unused) dependencies. Categorizes dependencies by domain (http, logging, testing, serialization, database, CLI, async, validation, crypto, web framework) using well-known package name matching for each ecosystem.
- Files changed:
  - crates/seshat-scanner/Cargo.toml (added serde, serde_json, toml deps)
  - crates/seshat-scanner/src/error.rs (added ManifestError variant to ScanError)
  - crates/seshat-scanner/src/lib.rs (added manifest module + re-exports)
  - crates/seshat-scanner/src/manifest.rs (NEW: full manifest analysis — 29 unit tests)
  - .ralph/prd.json (US-007 passes: true)
- **Learnings for future iterations:**
  - Cargo.toml dependency values are polymorphic: string `"1.0"`, table `{ version = "1.0" }`, or `dep.workspace = true` — use `HashMap<String, toml::Value>` and extract version from each form
  - PEP 508 specifiers: package name is everything before the first `[`, `>`, `<`, `=`, `!`, `;`, `~` etc.; hyphens normalise to underscores
  - pyproject.toml PEP 621: main deps in `[project].dependencies`, dev deps heuristic via `[project.optional-dependencies]` groups named "dev"/"test"/"testing"
  - Cross-referencing Rust imports: `use serde::Serialize` → module is "serde::Serialize", so match on starts_with("dep_name::") as well as exact match
  - JS/TS npm imports: `import ... from 'react-dom/client'` → match dep name "react-dom" with module prefix "react-dom/"
  - Python imports: `from requests.auth import HTTPBasicAuth` → module "requests.auth" matched against dep "requests" via starts_with("requests.")
  - All 369 workspace tests pass, clippy clean
---

## 2026-03-26 23:30 (EET) - US-008
- What was implemented: Module structure detection and dependency graph construction (`module_structure.rs` in seshat-scanner). Detects modules by grouping parsed files by directory. Creates KnowledgeNode (Fact nature, Info weight) for each module with ext_data containing module path, file count, languages, and file list. Builds DependsOn edges from import relationships between modules by resolving import paths (relative, crate::, Python dotted, JS/TS index barrel) to target module directories. Builds PartOf edges for module hierarchy (submodule → parent module). Deduplicates DependsOn edges per module pair. Provides O(1) queryability via `dependencies_of(path)` and `dependents_of(path)` methods on ModuleGraph using stored node ID mappings. Integration test uses dedicated multi-directory Rust fixture project.
- Files changed:
  - crates/seshat-scanner/src/lib.rs (added module_structure module + re-exports)
  - crates/seshat-scanner/src/module_structure.rs (NEW: build_module_graph, ModuleGraph, ModuleInfo, import resolution — 27 unit tests)
  - crates/seshat-scanner/tests/fixtures/module_project/src/main.rs (NEW: fixture entry point)
  - crates/seshat-scanner/tests/fixtures/module_project/src/handlers/api.rs (NEW: fixture handler with cross-module imports)
  - crates/seshat-scanner/tests/fixtures/module_project/src/handlers/web.rs (NEW: fixture handler)
  - crates/seshat-scanner/tests/fixtures/module_project/src/models/user.rs (NEW: fixture model)
  - crates/seshat-scanner/tests/fixtures/module_project/src/utils/format.rs (NEW: fixture utility)
  - crates/seshat-scanner/tests/module_structure_integration.rs (NEW: 16 integration tests)
  - .ralph/prd.json (US-008 passes: true)
- **Learnings for future iterations:**
  - Module detection = directory containing source files. Each unique parent directory of a parsed file is a module.
  - Import resolution needs multiple strategies: relative paths, Rust `crate::` / `super::` / `self::`, Python dotted imports, JS/TS index/barrel imports
  - Must register directory-level paths in the import target map (not just file-level) — `crate::models` resolves to `src/models` directory, not a specific file
  - HashMap iteration order is non-deterministic — use BTreeMap for node ID assignment, and store path↔nodeId maps in the result struct for reliable queries
  - DependsOn edge deduplication is critical — multiple files in module A may import from module B, but we want one edge per (A, B) pair
  - PartOf edges should only link to parent directories that are themselves detected modules (contain source files)
  - `parse_file` returns `ProjectFile` directly (not `Result`) — no `.expect()` needed when using it
  - All 412 workspace tests pass, clippy clean
---
