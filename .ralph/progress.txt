# Ralphy Progress Log
Started: суббота,  2 мая 2026 г. 22:19:14 (EEST)

## Codebase Patterns
- Use `gix` (v0.72) for git operations — already workspaced and used in seshat-graph
- Response data types derive `#[derive(Debug, Clone, Serialize)]`, use `#[serde(skip_serializing_if = "Option::is_none")]` on optional fields
- MCP tool modules live in `crates/seshat-mcp/src/tools/`, registered in `tools/mod.rs`, with `impl_tool_request!` in `server.rs`
- All tool handlers wrap results in `ResponseEnvelope::success` / map errors via `map_graph_error()`
- Enums use `#[serde(rename_all = "snake_case")]` for serialization
- Internal query helpers in `GraphError::query()` use `format!()` for error messages
- Tests use `tempfile::tempdir()` for isolated git repos, `Command::new("git")` for git operations
- `load_branch_ir(conn, branch_id)` in `code_pattern.rs` loads IR from files_ir table, shared by graph modules
- dependency analysis uses suffix matching at path boundaries to handle absolute vs relative paths
- Blast radius classification uses same thresholds everywhere: <=2 low, 3-10 medium, >10 high

## 2026-05-03 01:30 (EEST) - US-001
- Verified all types already defined: ChangedFile, FileStatus, AffectedSymbol, DependentRef, ConventionRisk, AdoptionSummary, BlastRadiusSummary, ImpactMetadata, DiffImpactData, DiffImpactRequest
- get_changed_files() already fully implemented with gix diff, conflict detection, untracked detection, staged_only/base support
- Module diff_impact already registered in lib.rs with re-exports
- cargo build/clippy --workspace passes, 183 seshat-graph + 138 seshat-mcp tests pass
- **Learnings for future iterations:**
  - diff_impact.rs already has placeholder types for AffectedSymbol, ConventionRisk, etc. that US-002/US-003 need to fill in with actual compute functions
  - gix is already a workspace dependency and used in seshat-graph for git operations
  - Integration tests have pre-existing migration failure (V9__drop_redundant_embedding_index missing) — not related to changes
  - The existing types serve as the structural contract for subsequent stories
---

## 2026-05-03 01:35 (EEST) - US-002
- Added `query_dependencies_batch(conn, branch_id, paths: &[String]) -> Vec<DependencyData>` to `dependencies.rs` — loads IR once, builds dependents for all paths O(N)
- Added `compute_affected_symbols(conn, branch_id, changed_files: &[ChangedFile]) -> Vec<AffectedSymbol>` to `diff_impact.rs` — extracts exports + public functions from each changed file's IR, calls query_dependencies_batch
- Each AffectedSymbol includes name, file, kind ("export"/"function"), dependent_count, dependents (up to 5), blast_radius
- Excludes deleted, untracked, and conflicted files from analysis
- Added `classify_blast_radius()` to `diff_impact.rs` using same thresholds as `dependencies.rs` (<=2 low, 3-10 medium, >10 high)
- Exported new functions from `lib.rs`
- Files changed: `crates/seshat-graph/src/dependencies.rs`, `crates/seshat-graph/src/diff_impact.rs`, `crates/seshat-graph/src/lib.rs`
- **Learnings for future iterations:**
  - `compute_affected_symbols` loads IR independently (not via `load_branch_ir` from the same conn guard) — each function call loads its own copy; acceptable for now since the IR size is bounded (10k files max)
  - The `DependentRef` struct in `diff_impact.rs` has `file` and `line` fields; `DependentEntry` from `dependencies.rs` has `file_path`, `import_names`, `line` — need to map between them
  - US-003 (convention risks) and US-004 (orchestration) are the next steps; the data flow is: US-001 (get changed files) → US-002 (compute affected symbols) → US-003 (compute convention risks) → US-004 (orchestrate + summary)
---

## 2026-05-03 01:50 (EEST) - US-003
- Added `compute_convention_risks(conn, branch_id, changed_files) -> Vec<ConventionRisk>` to `diff_impact.rs`
- SQL query uses `json_each(json_extract(n.ext_data, '$.evidence'))` to batch-match changed files against convention evidence
- Filters: only conventions where `weight IN ('rule','strong')` OR `adoption_count >= 3`; excludes soft-deleted nodes
- Deduplication by (description, affected_file) pair via HashSet
- Three distinct note styles: standard ("contributes evidence"), golden file ("highest compliance score"), deleted file ("convention confidence may decrease")
- Golden file detection via `get_golden_files()` (top compliance-scored files from files_ir)
- Excludes untracked and conflicted files; deleted files get special note but no golden flag
- Exported `compute_convention_risks` from `lib.rs`
- Files changed: `crates/seshat-graph/src/diff_impact.rs`, `crates/seshat-graph/src/lib.rs`
- **Learnings for future iterations:**
  - `get_golden_files()` requires `BranchId` from seshat-core — already a workspace dependency
  - Evidence matching uses suffix match (ev_file.ends_with("/{path}")) since IR paths can be absolute while git paths are relative
  - Confidence is stored as 0.0-1.0 float in nodes.confidence, surfaced as pct (multiplied by 100 and rounded)
  - US-004 (map_diff_impact orchestration) is next — will wire: get_changed_files → compute_affected_symbols → compute_convention_risks → summary + next_steps
---

## 2026-05-03 02:10 (EEST) - US-004
- Implemented `map_diff_impact(conn, branch_id, repo_path, request) -> DiffImpactData` — orchestrates get_changed_files → compute_affected_symbols → compute_convention_risks
- Added `compute_overall_risk()` — aggregates max blast radius across affected symbols (none/low/medium/high)
- Added `generate_next_steps()` — context-aware suggestions: review high-impact symbols (>=3 dependents), golden file hint, deleted file verify, run test suite, validate_approach
- metadata.branch uses branch_id from ProjectConnection (already resolved by scope)
- Exported `map_diff_impact` from `lib.rs`
- Files changed: `crates/seshat-graph/src/diff_impact.rs`, `crates/seshat-graph/src/lib.rs`
- **Learnings for future iterations:**
  - The `map_diff_impact()` function takes the existing `DiffImpactRequest` (which already has staged_only, base, repo_path) — no new request type needed
  - Branch name comes from `ProjectConnection.branch` in the MCP layer; the graph layer receives it as a plain string
  - Next steps generation is all local logic — no additional DB queries needed beyond what US-002/US-003 already do
   - US-005 (MCP handler) is next — needs a new `MapDiffImpactRequest` type with repo/scope/file_path fields matching the ToolRequest trait
---
## 2026-05-03 23:29 (EEST) - US-005
- Created `crates/seshat-mcp/src/tools/diff_impact.rs` with `MapDiffImpactRequest` (staged_only, base, repo_path fields + standard repo/scope/file_path) and `handle()` function
- Input validation: staged_only + base together → InvalidInput error; missing repo_path → InvalidInput error; not a git repo → InvalidInput error
- Calls `seshat_graph::map_diff_impact(conn, branch, &repo_path, &graph_request)` and wraps result in `ResponseEnvelope::success` with metadata extras (changed_files_count, affected_symbols_count, convention_risks_count, risk)
- Registered `pub mod diff_impact;` in `tools/mod.rs`
- Added import, `impl_tool_request!(MapDiffImpactRequest);`, `#[tool]` method in `#[tool_router]` block, call logger match arm, and ServerHandler instructions update in `server.rs`
- Added `diff_impact_result()` to `call_logger.rs` extracting changed_file_count, affected_symbol_count, convention_risk_count, blast_radius
- All 183 seshat-graph + 138 seshat-mcp tests pass, cargo build/clippy clean
- Files changed: `crates/seshat-mcp/src/tools/diff_impact.rs` (new), `crates/seshat-mcp/src/tools/mod.rs`, `crates/seshat-mcp/src/server.rs`, `crates/seshat-mcp/src/call_logger.rs`
- **Learnings for future iterations:**
  - The MCP tool layer adds a separate `MapDiffImpactRequest` with standard `repo/scope/file_path` fields, which is separate from the graph layer's `DiffImpactRequest` — the handle function maps between them
  - `repo_path` is required in the MCP layer since there's no auto-detection; validation uses `.git` directory existence check
  - The `#[tool]` description string is very long — it's displayed to AI agents as tool documentation, so it should be comprehensive
   - US-006 (skills/rules/hooks updates) is next — needs to modify agent guidance markdown files
---

## 2026-05-04 11:32 (EEST) - US-006
- Updated `skills/seshat/SKILL.md`: Added workflow step 7 ("Before committing or during code review") with `map_diff_impact()` usage, updated "All Tools" heading to "All 9 Tools", added `map_diff_impact` table row
- Updated `rules/seshat.md`: Added trigger row "Before committing or during code review" → `map_diff_impact()`
- Created `.config/opencode/AGENTS.md`: New file with full seshat trigger table including `map_diff_impact` row
- Updated `rules/hooks/seshat-session-start`: Added `map_diff_impact(repo_path="...")` to reminder list
- cargo build --workspace and cargo clippy --workspace pass cleanly
- Files changed: `skills/seshat/SKILL.md`, `rules/seshat.md`, `.config/opencode/AGENTS.md` (new), `rules/hooks/seshat-session-start`
- **Learnings for future iterations:**
  - No `.config/opencode/` directory existed before — had to create it and the AGENTS.md from scratch
  - AGENTS.md uses the `<!-- seshat:start -->` / `<!-- seshat:end -->` delimiters like CLAUDE.md files to allow tooling to inject/update content between delimiters
  - SKILL.md table header was "All 8 Tools" — updated to "All 9 Tools" to match the new count
---

## 2026-05-04 23:55 (EEST) - US-007
- Added 9 graph-layer integration tests to `diff_impact.rs` covering all acceptance criteria:
  - No uncommitted changes → empty result, risk: none, "nothing to review" next steps
  - Modified file with 0 exports → changed_files: [1], affected_symbols: [], risk: none
  - Modified file with exported fn having 5 dependents → affected_symbols: [{dependent_count: 5, blast_radius: medium}], risk: medium
  - Deleted file → status: deleted, excluded from affected_symbols
  - Untracked file (not in IR) → status: untracked, excluded from affected_symbols and convention_risks
  - Merge conflict file → status: conflicted, excluded from all analysis
  - Golden file modified → convention_risks.is_golden_file: true, note without WARNING, mentions record_decision
  - Detached HEAD → no error (handled gracefully)
- Added 6 MCP-layer validation tests to `tools/diff_impact.rs`:
  - staged_only + base → error "mutually exclusive"
  - Missing/nil repo_path → error "repo_path is required"
  - Empty/whitespace repo_path → error
  - Non-git directory → error "Not a git repository"
  - Valid repo → success with proper data structure
  - Detached HEAD → success (not an error)
- **BUG FIX:** Fixed deadlock in `compute_convention_risks()` — it was calling `get_golden_files()` while holding `conn_guard` (Rust mutex not reentrant). Moved `get_golden_files()` call BEFORE `lock_conn()`. This bug was never caught because no tests exercised the golden-file code path before.
- Files changed: `crates/seshat-graph/src/diff_impact.rs`, `crates/seshat-mcp/src/tools/diff_impact.rs`
- **Learnings for future iterations:**
  - **CRITICAL**: `get_golden_files()` calls `lock_conn()` internally (it acquires the mutex). Never call it from a scope that already holds `conn_guard` — Rust's std Mutex is non-reentrant and will deadlock. Always release the guard before calling any function that might re-acquire it.
  - When inserting IR files for golden-file tests, use raw SQL INSERT with explicit `convention_compliance_count` column — `insert_ir()` helper doesn't set it.
  - All 21 new tests (15 graph + 6 MCP) pass; 2 pre-existing scan_integration tests still fail due to missing V9 migration (unrelated).
  - US-008 (integration verification) is next — run full cargo test and clippy, manual testing, update epics.md.
---
