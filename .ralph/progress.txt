# Ralphy Progress Log
Started: понедельник, 30 марта 2026 г. 23:19:52 (EEST)
---

## Codebase Patterns
- Config module (AppConfig) lives in seshat-cli, not seshat-bin — bin stays thin
- build.rs for GIT_HASH lives in seshat-cli (same crate as clap types that use it)
- scan_project() has two variants: scan_project() (no progress) and scan_project_with_progress() (with callback)
- ScanProgress enum has 4 variants: Discovering, DiscoveryDone, Scanning, ScanningDone
- Convention detection runs AFTER scan pipeline: load files from DB → run_all_detectors → aggregate_findings
- DB path follows XDG convention: dirs::data_dir()/seshat/repos/{project_name}.db
- tracing-subscriber uses SESHAT_LOG env var, defaults to "warn" to avoid cluttering CLI output
- Use `eprintln!` for CLI status output (not println!) so it doesn't mix with potential JSON/structured stdout
- Pre-commit hooks run: trim whitespace, fix EOF, check toml, cargo fmt, cargo clippy
- cargo fmt may reformat imports (alphabetical) and assert! macros — always stage after fmt hook
- All format functions take `color: bool` parameter; caller passes `format::color_enabled()` once at top of command
- Verbosity enum: from_flags(verbose, quiet) → Quiet/Default/Verbose; quiet wins if both set
- Box-drawing chars (─, ▓, ░, ┌, ┐, │, └, ┘) are multi-byte UTF-8 — use .chars().count() not .len() for display width
- format.rs is the SINGLE module for all CLI output formatting — US-003/US-004 must use these helpers
- Report rendering lives in seshat-cli/src/report/ — separate from scan logic in scan.rs
- ReportData struct is the boundary: scan.rs builds it, report/ renders it — report code never queries DB
- Dependency counts come from ScanResult.manifest_analyses (ManifestAnalysis), NOT from ProjectFile.dependencies_used (which is always empty when loaded from DB)
- ScanResult.manifest_analyses was added in US-003 — previous iterations only had manifests_analyzed count
- discover_manifests() only looks at root-level manifests — workspace Cargo.toml with only [workspace.dependencies] returns 0 deps
- AggregatedConvention.confidence is a 0.0-1.0 float; multiply by 100.0 before ConfidenceTier::from_confidence()
- New report sections: create report/{section}.rs, add `pub mod` in report/mod.rs, call from print_report()
- Integration tests live in crates/seshat-bin/tests/ using assert_cmd + predicates (test the binary end-to-end)
- styled_tier_bullet(tier, color) in format.rs returns the colored bullet string — use for both tier summaries and finding lines
- Always run `cargo doc --no-deps --document-private-items` to catch broken intra-doc links
---

## 2026-03-31 00:30 (EEST) - US-001
- What was implemented:
  - clap v4 with derive-based subcommand parsing (Cli struct, Command enum)
  - seshat scan <path> with path validation, error + hint messages
  - seshat --version/-V prints "seshat {version} ({git_hash})"
  - Version header at scan start
  - indicatif two-phase progress: spinner for discovery, progress bar for scanning
  - ScanProgress enum + scan_project_with_progress() callback mechanism
  - Full end-to-end pipeline: discovery → parse → detect → aggregate
  - DB in XDG data directory (dirs::data_dir()/seshat/repos/{project_name}.db)
  - AppConfig loaded from seshat.toml or defaults
  - tracing-subscriber with SESHAT_LOG env filter
  - Stubbed serve/status/review/init with "not yet implemented" + exit(1)
  - Config module moved from seshat-bin to seshat-cli
  - seshat-bin is now thin: just calls seshat_cli::run()
- Files changed:
  - Cargo.toml (workspace deps: clap, indicatif, tracing-subscriber)
  - crates/seshat-bin/Cargo.toml (slimmed to just seshat-cli dep)
  - crates/seshat-bin/src/main.rs (thin wrapper)
  - crates/seshat-bin/build.rs (removed — moved to seshat-cli)
  - crates/seshat-bin/src/config.rs (removed — moved to seshat-cli)
  - crates/seshat-cli/Cargo.toml (added clap, indicatif, tracing-subscriber, dirs, etc.)
  - crates/seshat-cli/build.rs (new — captures GIT_HASH)
  - crates/seshat-cli/src/lib.rs (run() entry point with tracing init + command dispatch)
  - crates/seshat-cli/src/args.rs (new — Cli/Command clap types)
  - crates/seshat-cli/src/config.rs (moved from seshat-bin)
  - crates/seshat-cli/src/scan.rs (new — scan command implementation)
  - crates/seshat-cli/src/error.rs (updated doc comments)
  - crates/seshat-scanner/src/orchestrator.rs (ScanProgress enum + scan_project_with_progress)
  - crates/seshat-scanner/src/lib.rs (re-export ScanProgress + scan_project_with_progress)
- **Learnings for future iterations:**
  - The scan pipeline in orchestrator.rs does NOT run detectors — detection is a separate step after scan
  - Must reload files from DB after scan to get the complete set for detection (including unchanged files from incremental)
  - aggregate_findings requires file_dates HashMap and current timestamp for trend computation
  - The discovery phase is synchronous and fast — spinner barely visible on small projects
  - seshat-cli now has both seshat-scanner AND seshat-detectors as dependencies (needed for full pipeline)
  - Config tests exercise env var override which requires unsafe env::set_var in Rust 2024 edition
---

## 2026-03-31 01:15 (EEST) - US-002
- What was implemented:
  - owo-colors v4 added to workspace dependencies and seshat-cli crate
  - New seshat-cli/src/format.rs module with shared formatting utilities:
    - format_section_header() — box-drawing section headers padded to 60 chars
    - format_bar_chart() — horizontal bar charts with ▓/░ characters, percentage, label, count
    - format_tier_bullet() — confidence tier bullets (● high, ◐ medium, ○ low) with color
    - format_human_size() — human-readable file sizes (B/KB/MB/GB)
    - format_number() — thousands separator formatting
    - format_error_hint() — error + hint message formatting
    - format_bordered_box() — bordered box with ┌─┐│└─┘ characters for code/config snippets
    - format_warn() / format_info() — level-prefixed messages
    - color_enabled() — NO_COLOR environment variable check
    - ConfidenceTier enum with from_confidence() classifier
  - Verbosity enum: Quiet/Default/Verbose with from_flags(), show_warnings/findings/verbose methods
  - NO_COLOR support: color_enabled() checks env once, result threaded as `color: bool` to all format functions
  - scan.rs updated to use Verbosity and formatting:
    - --quiet hides version header, progress bars, manifests line, DB size, warnings
    - --verbose adds timing breakdown section
    - Summary line always shown (even in quiet mode)
    - DB size displayed with human-readable format
    - Numbers formatted with thousands separators
    - Progress bars hidden via ProgressDrawTarget::hidden() in quiet mode
  - 35 unit tests for format.rs covering all functions, color/no-color, verbosity flags
- Files changed:
  - Cargo.toml (workspace deps: added owo-colors)
  - Cargo.lock (added owo-colors v4.3.0)
  - crates/seshat-cli/Cargo.toml (added owo-colors dependency)
  - crates/seshat-cli/src/format.rs (new — all formatting utilities)
  - crates/seshat-cli/src/lib.rs (added format module, re-export Verbosity)
  - crates/seshat-cli/src/scan.rs (refactored to use Verbosity + format utilities)
- **Learnings for future iterations:**
  - Box-drawing characters (─, ▓, ░, etc.) are 3 bytes each in UTF-8; use .chars().count() for display width, not .len()
  - owo-colors uses method syntax (`.red()`, `.bold()`); all styling must go through the `color: bool` parameter
  - NO_COLOR is checked once via color_enabled() and passed down — don't re-check in each format function
  - format_section_header uses HEADER_WIDTH=60 as character count, not byte count
  - Progress bars can be hidden via `set_draw_target(ProgressDrawTarget::hidden())` instead of conditional creation
  - scan.rs print_scan_report() extracted as separate function to keep run_scan() readable
---

## 2026-03-31 02:00 (EEST) - US-003
- What was implemented:
  - New seshat-cli/src/report/ module directory with mod.rs and overview.rs
  - ReportData struct: pre-computed data boundary between scan logic and report rendering
  - build_report_data() collects language breakdown, dependency counts from manifest analysis, DB size
  - LanguageCount and EcosystemCount types for structured data passing
  - Project Overview section with format_section_header("Project Overview")
  - Language breakdown with horizontal bar charts (▓/░), percentage, file count — sorted descending
  - Dependency count with ecosystem breakdown: "N packages (X cargo, Y npm)"
  - Dependencies sourced from ScanResult.manifest_analyses (ManifestAnalysis) instead of ProjectFile.dependencies_used
  - ScanResult extended with `manifest_analyses: Vec<ManifestAnalysis>` field in orchestrator.rs
  - Orchestrator updated to populate manifest_analyses from analyze_manifests() result
  - print_scan_report() in scan.rs replaced with report::build_report_data() + report::print_report()
  - Report module respects Verbosity: overview shown in Default+Verbose, hidden in Quiet
  - Zero-count ecosystems filtered from dependency breakdown display
  - 12 new unit tests in report/mod.rs and report/overview.rs
- Files changed:
  - crates/seshat-cli/src/report/mod.rs (new — ReportData, build_report_data, print_report, tests)
  - crates/seshat-cli/src/report/overview.rs (new — print_overview, format_dependency_detail, tests)
  - crates/seshat-cli/src/lib.rs (added report module)
  - crates/seshat-cli/src/scan.rs (replaced print_scan_report with report module calls)
  - crates/seshat-scanner/src/orchestrator.rs (added manifest_analyses to ScanResult, populate from analyze_manifests)
- **Learnings for future iterations:**
  - ProjectFile.dependencies_used is ALWAYS empty when loaded from DB (ir_serialization.rs hardcodes Vec::new())
  - Manifest dependency data must come from ManifestAnalysis, not from ProjectFile fields
  - discover_manifests() only reads root-level manifest files — workspace Cargo.toml with only workspace.dependencies has 0 direct deps
  - The report module pattern (ReportData as boundary) makes it easy to add new sections without touching scan.rs
  - When adding fields to ScanResult, update all test constructions across the workspace (found in report/mod.rs tests)
---

## 2026-03-31 03:00 (EEST) - US-004
- What was implemented:
  - New seshat-cli/src/report/conventions.rs module with:
    - print_conventions() — Conventions Detected section with tier summary and findings list
    - print_next_steps() — Next Steps section with copy-paste commands
  - Section header: "── Conventions Detected ({count}) ──" using format_section_header()
  - Confidence tier summary: ● High (N)  ◐ Medium (N)  ○ Low (N) using format_tier_bullet()
  - Top findings list sorted by confidence descending:
    - Tier bullet + description (40-char padded) + trend arrow + confidence % + (detector_name)
  - Trend indicators: ↑ Rising, ─ Stable, ↓ Declining, (space) Unknown
  - Default mode shows top 10 conventions; --verbose shows all; "... and N more" hint when truncated
  - Next Steps section: `seshat review`, `seshat serve`, `seshat init` commands
  - Summary line: "{count} conventions detected. Run `seshat review` to validate."
  - Database path with human-readable size: "Database: {path} ({size})"
  - Completion time always shown: "Completed in {time}s"
  - print_report() restructured: scan stats → overview → conventions → next steps → summary → DB path → timing
  - 16 unit tests: count_tiers (4), trend_indicator (4), print_conventions (5), print_next_steps (2), plus integration
- Files changed:
  - crates/seshat-cli/src/report/conventions.rs (new — conventions and next steps rendering)
  - crates/seshat-cli/src/report/mod.rs (added conventions module, restructured print_report flow)
- **Learnings for future iterations:**
  - AggregatedConvention.confidence is 0.0-1.0 (not 0-100%); multiply by 100 before passing to ConfidenceTier::from_confidence()
  - Trend enum from seshat_core has 4 variants: Rising/Stable/Declining/Unknown — use Unicode arrows ↑─↓ for display
  - Report sections are added by: (1) create submodule in report/, (2) declare `pub mod` in mod.rs, (3) call from print_report()
  - The report module test helper make_report_data* pattern is duplicated across overview.rs and conventions.rs — could be extracted to a shared test helper if more sections are added
  - Conventions are sorted at display time (in conventions.rs), not at data-build time — keeps ReportData order-agnostic
---

## 2026-03-31 04:30 (EEST) - US-005
- What was implemented:
  - Full code review and quality gate for all changes from US-001 through US-004
  - All automated checks pass: cargo fmt, cargo clippy (-D warnings), cargo test, cargo doc
  - Added doc comments to all public modules in lib.rs (6 pub mod declarations)
  - Added doc comments to pub mod declarations in report/mod.rs (conventions, overview)
  - Added doc comment to Cli.command field in args.rs
  - Extracted styled_tier_bullet() and tier_bullet_char() helpers from conventions.rs to format.rs
  - Removed duplicated bullet-styling logic from conventions.rs print_convention_line()
  - Removed unused TierColor enum from format.rs (superseded by styled_tier_bullet)
  - Fixed 2 broken intra-doc links: color_enabled() and ReportData in module-level docs
  - Added 13 integration tests in crates/seshat-bin/tests/scan_integration.rs:
    - Error cases: nonexistent path, file-instead-of-directory
    - Success cases: empty dir with warning, fixture project scan, no-parseable-files dir
    - Verbosity: quiet mode minimal output, verbose mode shows timing
    - Version flag, no-subcommand help, stubbed commands (serve/status/review/init)
  - Added assert_cmd, predicates, tempfile as workspace + dev-dependencies
  - No unsafe blocks in library code (only in test config env var manipulation)
  - Two .expect() in scan.rs on hardcoded ProgressStyle templates — acceptable (compile-time constants)
  - Error handling uses thiserror throughout, ? for propagation
  - CLI types use proper Rust types: PathBuf, enums, bool flags
  - seshat-cli depends on seshat-storage (intentional — CLI orchestrates full pipeline)
  - No N+1 queries — report data built from in-memory structures
  - Formatting logic consolidated in format.rs — no copy-paste between report sections
- Files changed:
  - Cargo.toml (added assert_cmd, predicates to workspace deps)
  - Cargo.lock (added assert_cmd, predicates, and transitive deps)
  - crates/seshat-bin/Cargo.toml (added dev-dependencies: assert_cmd, predicates, tempfile)
  - crates/seshat-bin/tests/scan_integration.rs (new — 13 integration tests)
  - crates/seshat-cli/src/lib.rs (added doc comments to all pub mod declarations)
  - crates/seshat-cli/src/args.rs (added doc comment to Cli.command field)
  - crates/seshat-cli/src/format.rs (extracted styled_tier_bullet, tier_bullet_char; removed TierColor; fixed broken doc link)
  - crates/seshat-cli/src/report/mod.rs (added doc comments to pub mod; fixed broken doc link)
  - crates/seshat-cli/src/report/conventions.rs (refactored to use styled_tier_bullet from format.rs)
- **Learnings for future iterations:**
  - assert_cmd is the standard Rust crate for CLI integration testing — works with cargo_bin("seshat")
  - predicates crate provides fluent assertions on stdout/stderr content
  - cargo doc --document-private-items catches broken intra-doc links that normal builds miss
  - Module-level //! doc links to items in the same module need `self::` prefix or backtick-only formatting
  - cargo fmt may reorder imports within a use group — always run fmt after editing imports
  - The two .expect() calls on ProgressStyle::with_template() are acceptable for hardcoded string literals
---
