# Ralphy Progress Log
Started: среда, 13 мая 2026 г. 18:00:32 (EEST)
---

## Codebase Patterns
- `query_code_pattern` enrichment passes (call-sites, dependent_files) live in
  `crates/seshat-graph/src/code_pattern.rs` as separate functions invoked
  AFTER the merged keyword + vector result set is finalised.  Each
  enrichment is best-effort: a SQL failure downgrades to empty rather
  than aborting the whole query.  When extending the per-pattern response
  shape, add another enrichment step in the same place rather than
  weaving the new lookup into the keyword/vector search paths.
- Read-side SQL probes against `symbol_definitions` / `symbol_imports`
  live directly in `seshat-graph::code_pattern` (e.g.
  `search_symbol_definitions`, `enrich_with_dependent_files`) — they
  hold the connection lock via `crate::lock_conn` and use the same
  V13 `(branch_id, name)` / `(branch_id, imported_name)` indexes the
  write side already maintains.  The `SymbolIndexRepository` trait
  stays write-/lifecycle-focused (replace_file, delete_file,
  delete_branch, counts).
- Migrations live in `crates/seshat-storage/migrations/V<N>__<name>.sql`,
  embedded via `refinery::embed_migrations!`. Refinery's SQL files cannot
  deserialize postcard blobs — for backfill that needs Rust, perform it in
  `Database::open` immediately after `migrations::runner().run(...)`, gated
  on a "table empty?" check so it stays idempotent across reopens.
- IR snippets must be built the same way wherever they are produced. Synthetic
  definition snippet helpers (function/type/export) live in
  `seshat-core::symbol_snippet` so `seshat-storage` (writer) and
  `seshat-graph` (reader) emit identical strings without one crate depending
  on the other.
- Repository traits live in `crates/seshat-storage/src/repository/mod.rs`;
  Sqlite impls live in sibling files. Re-export everything via
  `crates/seshat-storage/src/lib.rs` so downstream crates only import from the
  crate root.
- Per-file index writes use the `replace_file` pattern: `DELETE` rows for
  `(branch_id, file_path)` then `INSERT` the new set, all in one
  `unchecked_transaction()`. Keeps re-scans / hot-tier updates idempotent.
- Tests that need to seed `files_ir` directly with serialized IR can use
  `crate::ir_serialization::serialize_ir(&project_file)` and bypass the
  repository.
- Cross-table writes that must commit atomically (e.g., `files_ir` + symbol
  index) live as a single method on the owning repository
  (`FileIRRepository::upsert_with_symbol_index`) that opens its own
  `unchecked_transaction()` and runs every statement inside it.  Sibling
  modules expose `pub(super)` SQL helpers operating on a `&Transaction` so
  the writer doesn't duplicate the per-table DELETE/INSERT SQL.
- Parser convention: `Import.names` stores defining (rightmost) names, never
  local aliases.  TS/JS already do this via the first identifier of
  `import_specifier`; Rust and Python parsers normalise top-level
  `use foo::Bar as Baz` and `import x as y` / `from m import x as y` to keep
  the IR consistent and downstream code (symbol-index, detectors) free of
  alias-stripping logic.
- Branch lifecycle ownership: every per-branch table must be touched by
  `BranchRepository::create_snapshot` (copy) and `delete_branch` (drop).
  After adding a new per-branch table, audit both methods — missing one
  produces silent state drift (e.g. symbol-index that doesn't survive
  branch switches, or orphan rows that accumulate across recreate cycles).
- Per-crate test helpers seed BOTH halves of a derived index in one call.
  After US-002 made `files_ir` and `symbol_definitions` share a single
  write transaction (`upsert_with_symbol_index`), tests that used to call
  the lower-level `INSERT INTO files_ir ...` directly leave the symbol
  index empty.  `seshat-graph`'s and `seshat-mcp`'s `insert_ir` helpers
  (and `seshat-mcp::server::insert_ir_for_server`) now also call
  `SqliteSymbolIndexRepository::replace_file(...)` so a single fixture
  call lands a complete row set.  When you add another per-file table to
  this DB family, update the helpers in lock-step.
- SQL `LIKE` patterns need escaping for `_` (single-char wildcard) and
  `%` (any-length wildcard).  Use `LIKE ? ESCAPE '\\'` plus a helper that
  prefixes `\\` before `_`, `%`, and `\\` itself.  Without escaping,
  `do_thing` would also match `doXthing` — fixed in
  `crates/seshat-graph/src/code_pattern.rs::escape_like_pattern`.
- Lazy IR load: code paths that load all of a branch's serialized IR
  should branch on "do we actually need it?".  `query_code_pattern_*`
  now skips `load_branch_ir` entirely when the keyword probe is empty AND
  no embedding provider is configured — saves a full deserialization
  pass on misses.
- Blast-radius classification is a single shared helper:
  `seshat_graph::dependencies::classify_blast_radius(count: usize) ->
  BlastRadius` with `< 5 / 5..=20 / > 20` thresholds.  Both file-level
  (`query_dependencies`) and symbol-level (`query_code_pattern`) outputs
  funnel through it, so the labels can't drift between tools.  When
  adding a new per-X risk signal, call this helper rather than inlining
  a comparison — keep the thresholds in one place.

## 2026-05-13 18:15 (EEST) - US-001
- Implemented schema + migration for `symbol_definitions` and `symbol_imports`
  tables, plus Rust-side backfill that runs in `Database::open` after refinery
  applies pending SQL migrations. Backfill is gated on
  "symbol_definitions empty AND files_ir non-empty" so reopening a populated
  DB is a no-op.
- Files added:
  - `crates/seshat-storage/migrations/V13__symbol_index.sql` — CREATE TABLE +
    indexes for both new tables.
  - `crates/seshat-storage/src/repository/symbol_index_repository.rs` —
    `SqliteSymbolIndexRepository`, `SymbolDefinitionRow`, `SymbolImportRow`,
    `SymbolKind`, plus `extract_definitions` / `extract_imports` helpers
    (filter wildcards `*` and `* as alias`).
  - `crates/seshat-core/src/symbol_snippet.rs` — synthetic snippet helpers
    shared between writer (storage) and reader (graph), with
    `MAX_DEFINITION_SNIPPET_LINES = 10`.
- Files changed:
  - `crates/seshat-core/src/lib.rs` — register/re-export `symbol_snippet`.
  - `crates/seshat-storage/src/repository/mod.rs` — register module + add
    `SymbolIndexRepository` trait.
  - `crates/seshat-storage/src/lib.rs` — re-export new symbol-index types.
  - `crates/seshat-storage/src/db.rs` — call `backfill_symbol_index(&conn)`
    after migrations; added 5 backfill tests + extended
    `migration_applies_on_fresh_in_memory_db` to assert the new tables and
    indexes.
- Tests: `cargo test -p seshat-storage` → 177 passed (added 11 new tests).
  `cargo clippy --workspace --all-targets -- -D warnings` clean.
  Full `cargo test --workspace` → all suites green.
- **Learnings for future iterations:**
  - Refinery 0.9 SQL migrations cannot run Rust; backfill that touches
    serialized blobs has to live in `Database::open` after the migrator runs,
    not in the `.sql` file itself. The AC's "inside the migration" wording is
    satisfied by the upgrade-time backfill being part of the V13 step.
  - The `SymbolKind::Export` row stores `is_public = true` unconditionally
    because exports are by definition externally-reachable — the IR
    `Export` struct has no `is_public` field.
  - Stale IR rows (older `ir_schema_version`) are skipped during backfill,
    matching how `get_file_hashes_by_branch` already excludes them. They will
    repopulate after the user runs `seshat scan`.
  - Wildcard imports filter is `name == "*" || name.starts_with("* as ")` —
    the second guard catches the TS parser's `import * as foo from '…'`
    output which stores `["* as foo"]` in `Import.names`.
  - `cargo fmt --all -- --check` flagged trailing-method splits across
    several test lines in `symbol_index_repository.rs`. Run `cargo fmt --all`
    before committing.
  - For US-002 the storage helpers `extract_definitions` /
    `extract_imports` are already in place and re-exported from
    `seshat_storage` — the scanner just needs to call them on the parsed
    `ProjectFile` and pass through `SymbolIndexRepository::replace_file`
    in the same transaction as `FileIRRepository::upsert`.
---

## 2026-05-13 18:40 (EEST) - US-002
- Wired the scanner orchestrator to populate `symbol_definitions` /
  `symbol_imports` for every parsed file in the same transaction as the
  `files_ir` UPSERT, and to drop matching rows whenever a file is removed.
  Single source of truth: parser → IR → orchestrator → repository.
- Files added:
  - `crates/seshat-scanner/tests/symbol_index_integration.rs` — 8 end-to-end
    tests (row counts match `extract_*` helpers, idempotency across re-scans,
    zero-row wildcard imports, per-language aliased-import defining-name
    storage, file deletion drops index rows).
- Files changed:
  - `crates/seshat-storage/src/repository/mod.rs` — new
    `FileIRRepository::upsert_with_symbol_index` trait method.
  - `crates/seshat-storage/src/repository/file_ir_repository.rs` —
    implementation: opens one `unchecked_transaction`, runs files_ir UPSERT
    + symbol-index DELETE/INSERT, commits or rolls back together.
  - `crates/seshat-storage/src/repository/symbol_index_repository.rs` —
    promoted `delete_definitions` / `delete_imports` / `insert_definitions`
    / `insert_imports` from private to `pub(super)` so the combined writer
    shares the SQL without duplication.
  - `crates/seshat-scanner/src/orchestrator.rs` — `scan_project_with_progress`
    builds a `SqliteSymbolIndexRepository`, calls
    `file_ir_repo.upsert_with_symbol_index(...)` for new/changed files, and
    calls `symbol_index_repo.delete_file(...)` for deleted files.
  - `crates/seshat-scanner/src/parser/python_parser.rs` — both
    `aliased_import` branches now store the defining (rightmost) name in
    `Import.names`, never the alias.  Two existing unit tests updated to
    pin the new contract.
  - `crates/seshat-scanner/src/parser/rust_parser.rs` — added
    `strip_as_alias` and apply it inside the top-level `use_as_clause`
    branch so `use std::fmt::Write as _;` produces `names=["Write"]`
    instead of `["Write as _"]`.
- Tests: `cargo test --workspace` → all green.
  `cargo clippy --workspace --all-targets -- -D warnings` clean.
  `cargo fmt --all -- --check` clean.
- Manual check: wiped the project DB and ran
  `seshat scan` against a copy of the seshat repo (~250 files):
  symbol_definitions=2443 (1539 function / 612 export / 292 type),
  symbol_imports=1822, **zero** rows with `*` or `... as ...` substrings —
  confirming the wildcard filter and the parser fixes hold against real
  Rust + Python + TS code.
- **Learnings for future iterations:**
  - On incremental re-scans, files with unchanged content hashes are not
    re-parsed and not upserted — their symbol-index rows therefore survive
    intact across runs.  This is exactly the idempotency property we want,
    but it also means parser-level changes (e.g. our alias-stripping fix)
    do **not** retroactively rewrite rows produced by older parsers until
    the file changes or `IR_SCHEMA_VERSION` bumps.  For manual smoke tests,
    wipe the DB before scanning to see fresh parser output.
  - `find_use_argument` in `rust_parser.rs` returns `use_as_clause` as the
    top-level argument when the entire `use_declaration` is an "X as Y"
    expression (e.g. `use std::fmt::Write as _;`).  The path inside
    `node_text` includes the trailing ` as <alias>` substring, which the
    use-list inner branch already skipped via `child.child(0)`.  Stripping
    is now factored into `strip_as_alias` and applied at the top-level
    branch — keeps both branches in sync.
  - TS/JS parsers were already correct: `import_specifier` exposes the
    defining identifier as `child(0)`.  Don't touch them.
  - The `Database::open` V13 backfill (US-001) only re-extracts from
    *existing* `files_ir` blobs — so if those blobs were produced by an
    older parser, the symbol-index inherits the old alias / wildcard
    bug.  A clean re-scan (or future schema bump) re-parses the source
    file and overwrites the bad rows via `upsert_with_symbol_index`.
  - For US-003 (watcher hot tier): `serve.rs:396` and the watcher's
    `hot_tier::process_file_change` should switch from
    `file_ir_repo.upsert` to `upsert_with_symbol_index`, and the file-delete
    path should also call `symbol_index_repo.delete_file`.  All of this
    machinery is now in place — US-003 is the wiring change.
---

## 2026-05-13 18:43 (EEST) - US-003
- Wired the watcher hot tier and the branch-switch/HEAD-change sync path to
  keep `symbol_definitions` / `symbol_imports` consistent with `files_ir` on
  every per-file event.  Also closed the snapshot gap: `create_snapshot` now
  copies symbol-index rows and `delete_branch` drops them, so a snapshotted
  branch starts with a full index and a wiped branch leaves no orphans.
- Files added:
  - `crates/seshat-watcher/tests/symbol_index_integration.rs` — 5 integration
    tests against `process_file_change` / `process_file_delete` covering the
    populate / replace / delete invariants and the exact "modify with
    `pub fn foo()` → row exists; remove the function → row is gone" wording
    in AC #4.  Driven through the same entry points the hot-tier task uses
    (no notify-debouncer-full timing dependency).
- Files changed:
  - `crates/seshat-watcher/src/hot_tier.rs` — `process_file_change` now calls
    `FileIRRepository::upsert_with_symbol_index` (one tx for `files_ir` +
    symbol-index halves); `process_file_delete` also calls
    `SymbolIndexRepository::delete_file` after the IR delete.
  - `crates/seshat-cli/src/serve.rs` — `incremental_sync_blocking` upserts
    via `upsert_with_symbol_index` for files whose oid differs between
    old/new branch tree, and also deletes symbol-index rows for files
    only present in the old tree.  `fallback_rescan` is unchanged because
    `scan_project` already owns symbol-index writes (US-002).
  - `crates/seshat-storage/src/repository/branch_repository.rs` —
    `create_snapshot` copies `symbol_definitions` and `symbol_imports` rows
    along with `files_ir`/`nodes`/`edges`; `delete_branch` drops them along
    with the rest of the branch's data.  Both happen inside the existing
    transactions, so a partial copy/delete cannot leave the index out of
    sync with the branches table.
- Tests: `cargo test --workspace` → all green (5 new integration tests +
  the existing 33 watcher unit tests, including `create_snapshot_copies_*`
  + `delete_branch_*` which keep proving the snapshot/delete invariants
  on `nodes` / `files_ir`).
  `cargo clippy --workspace --all-targets -- -D warnings` clean.
  `cargo fmt --all -- --check` clean.
- **Learnings for future iterations:**
  - `BranchRepository::create_snapshot` and `delete_branch` are the
    branch-lifecycle bottleneck.  When the schema grows a new per-branch
    table (V13 added two), both methods must be updated together.  The
    sync path (`incremental_sync_blocking`) handles only changed/added/
    removed files; for *unchanged* files in a freshly snapshotted branch
    the new branch inherits its state from the snapshot copy.  If
    `create_snapshot` doesn't copy a per-branch table, that table will
    be empty for unchanged files until the next full re-scan.
  - `process_file_delete` propagates `NotFound` from `delete_by_path`
    (existing behaviour, untouched here).  In practice the watcher can
    receive `Remove` events for files we never indexed (CSVs, hidden
    files), so warnings will fire — left as-is to keep this change
    minimally scoped to US-003.  If we want to silence them, the orchestrator
    already shows the pattern (`let _ = file_ir_repo.delete_by_path(...)`).
  - For US-004 (return `dependent_files` in `query_code_pattern`): the
    `symbol_imports` table is now reliably populated and kept in sync across
    full scans, hot-tier events, branch switches, and HEAD-change syncs.
    The blast-radius / dependent_files SQL described in US-004's AC ("SELECT
    DISTINCT importer_file FROM symbol_imports WHERE branch_id=? AND
    imported_name=?") will have an authoritative source against any branch
    after this change.
---

## 2026-05-13 19:00 (EEST) - US-009
- Routed `query_code_pattern`'s symbol matching through the V13
  `symbol_definitions` index.  The keyword path now issues one parameterised
  `SELECT ... FROM symbol_definitions WHERE branch_id = ? [AND kind = ?] AND
  LOWER(symbol_name) LIKE ? ESCAPE '\\'` per query token, scores the returned
  rows in Rust using the existing `score_name` (1.0 exact / 0.7 prefix / 0.4
  contains), and merges by `(file_path, name, kind)` taking the best score.
  No deserialized-IR iteration for definition lookup.
- Kind filter is pushed down as a SQL `WHERE` clause; the MCP handler no
  longer post-filters.  The graph layer normalises `Some("all")` /
  whitespace-only kinds to `None` so handler-side validation can stay the
  same.
- Old full-IR-scan helpers (`search_functions`, `search_types`,
  `search_exports`, plus their `function_snippet` / `type_snippet` /
  `export_snippet` wrappers and the IR-scan loop) are deleted entirely —
  no dormant code path.  The remaining `build_ir_lookup` for vector search
  now calls `seshat_core::*_definition_snippet` directly so writer + reader
  share one snippet implementation.
- IR is loaded lazily.  When the keyword probe finds nothing AND no embedding
  provider is configured, `load_branch_ir` is skipped entirely — a no-match
  query is the smallest possible work.
- Vector path keeps its IR-derived snippet lookup (the IR is already in
  memory for call-site enrichment, so re-pointing at `symbol_definitions`
  would add SQL roundtrips for no win).  Documented in an
  implementation comment per US-009 AC.
- Files added: none (refactor only).
- Files changed:
  - `crates/seshat-graph/src/code_pattern.rs` — replaced the IR-scan
    keyword loop with `search_symbol_definitions`; new `escape_like_pattern`
    helper; added `kind: Option<&str>` parameter to `query_code_pattern` /
    `query_code_pattern_with_embeddings`; lazy IR load.
  - `crates/seshat-graph/src/validate_approach.rs` — pass `None` for kind
    (duplicate detection wants matches across all kinds).
  - `crates/seshat-graph/src/lib.rs` — `test_helpers::insert_ir` now also
    writes `symbol_definitions` / `symbol_imports` via
    `SqliteSymbolIndexRepository::replace_file`.
  - `crates/seshat-mcp/src/lib.rs` — same change to the MCP-side
    `test_helpers::insert_ir`.
  - `crates/seshat-mcp/src/server.rs` — same change to
    `insert_ir_for_server`.
  - `crates/seshat-mcp/src/tools/query_code_pattern.rs` — pass the kind
    filter down to the graph layer; remove the post-filter `retain`.
- Tests added: 3 new tests in `crates/seshat-graph/src/code_pattern.rs`:
  - `kind_filter_pushed_into_sql_drops_other_kinds` — verifies the SQL
    `WHERE kind = ?` filters out other kinds, and that `Some("all")` /
    whitespace are treated as "no filter".
  - `sql_like_does_not_match_wildcard_underscore` — regression test for
    the LIKE-pattern escaping: `do_thing` must NOT match `doXthing`.
  - `lookup_time_bounded_with_1000_definitions` — seeds 50 files × 20
    symbols and asserts exact, prefix-with-kind, and no-match lookups all
    complete in under 200ms (generous CI-friendly upper bound; on a warm
    laptop these run in single-digit ms).
- `cargo test --workspace`: 32 code_pattern tests, 271 seshat-graph
  total, 185 seshat-mcp — all green.  `cargo clippy --workspace
  --all-targets -- -D warnings` clean.  `cargo fmt --all -- --check` clean.
- **Learnings for future iterations:**
  - SQL `LIKE` patterns *must* escape `_` and `%`.  Without `ESCAPE '\\'`
    and the escape helper, an underscored Rust identifier query becomes a
    silent contains-anything match.  The new
    `sql_like_does_not_match_wildcard_underscore` regression test pins
    this — keep it green when refactoring the keyword probe.
  - `seshat-graph` and `seshat-mcp` each carry their own private
    `test_helpers` module.  After making `query_code_pattern` SQL-backed,
    every fixture call that *only* writes `files_ir` becomes invisible
    to the keyword probe.  Audit all `insert_ir` helpers in the crate
    family and `insert_ir_for_server` in `server.rs` when the keyword
    path changes — this iteration found three separate copies that
    needed the mirror write to `symbol_definitions` / `symbol_imports`.
  - The graph layer normalises `Some("all")` / empty / whitespace-only
    kind values to `None` internally.  Handler-side validation still
    accepts `"all"` as a string for friendly error messages, and the
    graph layer is the single source of truth for "no filter" semantics.
    Don't add a second normalisation pass at the call site.
  - Vector + call-site paths still need `load_branch_ir`; only the
    keyword path moved off IR.  When extending the response with new
    per-symbol data (US-004's `dependent_files`, US-005's `blast_radius`,
    US-006's aggregated `call_sites`), prefer SQL probes against the
    symbol-index over loading IR — the only thing that *must* be in
    memory now is `function_calls` for call-site enrichment.
  - For US-004 (next story): the SQL is straight-forward —
    `SELECT DISTINCT importer_file FROM symbol_imports WHERE branch_id=?
    AND imported_name=?` per matched symbol, excluding `p.file_path`.
    The handler/graph plumbing for kind filtering is already in place;
    `dependent_files` just rides on the `PatternResult` shape.
---

## 2026-05-13 19:10 (EEST) - US-004
- Added `dependent_files: Vec<String>` to `PatternResult` and a new
  `enrich_with_dependent_files` step in
  `query_code_pattern_with_embeddings` that populates the field via one
  indexed SQL probe per pattern.  The defining file is excluded inside
  the SQL (`importer_file != ?3`), so a file that re-exports a symbol
  and also imports it never appears in its own dependents.  Re-export
  chains are not chased — only files with a direct concrete
  `use … ::Name` (or `from … import Name`, `import { Name }`) line
  count, which is exactly what V13's `symbol_imports` stores.
- Files changed:
  - `crates/seshat-graph/src/code_pattern.rs` — `PatternResult` gains
    `dependent_files`; new `enrich_with_dependent_files` helper; new
    enrichment step in `query_code_pattern_with_embeddings`; new tests
    (`dependent_files_lists_each_distinct_importer`,
    `dependent_files_excludes_defining_file`,
    `dependent_files_empty_for_private_symbol_never_imported`,
    `dependent_files_deduplicates_distinct_importer_per_file`,
    `dependent_files_isolated_per_branch`,
    `dependent_files_skips_wildcard_imports`).
  - `crates/seshat-mcp/src/tools/query_code_pattern.rs` — two new
    handler tests (`handle_response_includes_dependent_files_field`,
    `handle_response_dependent_files_populated_from_imports`) assert
    that every pattern entry in the JSON response carries the field
    and that the field is populated end-to-end from `symbol_imports`.
- Tests: `cargo test --workspace` green (38 code_pattern tests pass,
  including 6 new for US-004; 14 mcp::query_code_pattern tests pass,
  including 2 new).  `cargo clippy --workspace --all-targets -- -D
  warnings` clean.  `cargo fmt --all -- --check` clean.
- **Learnings for future iterations:**
  - The two write paths that own the `symbol_imports` table (full
    scan via `extract_imports` and the test helper `insert_ir`) both
    already filter wildcards and store defining names — so the read
    path doesn't need to re-filter or re-resolve aliases.  When the
    read query returns nothing for a public symbol, the right place
    to look is upstream (parser → IR → `extract_imports`), not in
    `enrich_with_dependent_files`.
  - Per-pattern SQL probes are cheap because the V13 index covers
    `(branch_id, imported_name)`.  We considered a single batched
    `WHERE imported_name IN (...)` query but the resulting Rust-side
    bookkeeping (dedupe per (name, file_path) tuple, per-pattern
    defining-file exclusion) was more complex than the savings
    warranted for typical query result counts (single-digit to low
    tens).  Re-evaluate if the enrichment ever shows up in a flame
    graph.
  - The enrichment is best-effort: a SQL error degrades the field to
    an empty list rather than aborting the whole query.  The rest of
    the response (call_sites, snippet, score, conventions) is still
    useful without dependent_files.
  - The existing `BlastRadius` enum thresholds in
    `crates/seshat-graph/src/dependencies.rs` are 0..3 = Low, 3..=10 =
    Medium, > 10 = High — NOT the 0..5 / 5..=20 / > 20 thresholds US-005's
    ACs mention.  US-005 should reconcile this discrepancy when it
    extracts the helper (either update the AC, update the helper, or
    introduce a separate threshold set for symbol-level blast — to be
    decided in that story).
  - For US-005 (blast_radius), the data source is already in place:
    `dependent_files.len()` on the merged result set.  The story is
    purely a wrapper field + classifier helper; the only architectural
    decision is which thresholds to pick (see point above).
  - For US-006 (aggregated call_sites by file): the existing
    `enrich_with_call_sites` walks `function_calls` IR and pushes to a
    flat list capped at `MAX_CALL_SITES_PER_PATTERN`.  Aggregation
    will need to track total count per file (no cap), keep the lines
    list per file, and emit the lowest-line first_snippet.  Be careful:
    the current code stops pushing call sites after the cap is hit but
    keeps incrementing `total_count`, so the per-file aggregation has
    to count BEFORE the cap to be accurate.
---

## 2026-05-13 19:21 (EEST) - US-005
- Added `blast_radius: BlastRadius` to `PatternResult` and populated it after
  the dependent_files enrichment using the shared
  `dependencies::classify_blast_radius` helper.  Resolved the AC discrepancy
  flagged in US-004's notes by updating the shared thresholds from `< 3 /
  3..=10 / > 10` to `< 5 / 5..=20 / > 20` — file-level (`query_dependencies`)
  and symbol-level (`query_code_pattern`) now use one helper, one set of
  cutoffs.  Pre-1.0 ⇒ no shim, no deprecation step.
- Files changed:
  - `crates/seshat-graph/src/dependencies.rs` —
    `BLAST_RADIUS_MEDIUM_THRESHOLD` 3 → 5, `BLAST_RADIUS_HIGH_THRESHOLD`
    10 → 20.  `BlastRadius` variant docstrings updated.
    `classify_blast_radius` is still `pub(crate)` so siblings inside
    `seshat-graph` can call it; doctring records the unified
    cross-tool semantics.  Existing `blast_radius_classification` test
    rewritten to the AC-required boundaries (0, 4, 5, 19, 20, 21, 100).
  - `crates/seshat-graph/src/code_pattern.rs` —
    `PatternResult` gains `pub blast_radius: BlastRadius`.  Step 7 in
    `query_code_pattern_with_embeddings` recomputes the field from
    `dependent_files.len()` for every pattern AFTER step 6's enrichment,
    so an enrichment failure (empty list) maps cleanly to `Low` rather
    than leaking a stale value.  Three `PatternResult` constructors
    (vector path, SQL keyword path, merge-test fixtures × 4) get the
    new field initialised to `BlastRadius::Low`.
  - `crates/seshat-graph/src/diff_impact.rs` — the single test that
    asserted `3 ⇒ Medium` (`transitive_dependent_count_uses_depth_3`)
    updated to `3 ⇒ Low` under the new shared thresholds.  The test's
    intent is BFS depth-3 expansion, not the Medium classification —
    semantics preserved, only the assertion changed.
  - `crates/seshat-mcp/src/tools/query_code_pattern.rs` — two new
    handler tests assert (a) every pattern entry carries a
    `blast_radius` string of `low|medium|high` and (b) seeding 5
    distinct importers yields `"medium"` end-to-end through the MCP
    JSON envelope.
- New tests in `code_pattern.rs`:
  - `blast_radius_boundary_4_is_low` / `_5_is_medium` /
    `_19_is_medium` / `_20_is_medium` / `_21_is_high` — exact AC
    boundaries 4/5/19/20/21 via the `blast_radius_for_importer_count`
    helper that seeds N distinct importers and asserts the resulting
    classification.
  - `blast_radius_private_symbol_never_imported_is_low` — pins
    `0 → Low` end-to-end so a symbol with zero importers doesn't leak
    a stale or uninitialised value.
  - `blast_radius_serializes_to_snake_case_string` — confirms
    `BlastRadius::Low` serialises to the literal JSON string `"low"`
    (the contract the MCP response shape relies on).
- Tests: `cargo test -p seshat-graph` → 284 passed (9 new); `cargo
  test -p seshat-mcp` → 189 passed (2 new).  Full `cargo test
  --workspace` green.  `cargo clippy --workspace --all-targets -- -D
  warnings` clean.  `cargo fmt --all -- --check` clean.
- **Learnings for future iterations:**
  - The AC's "Thresholds … identical to query_dependencies" wording
    was the architectural lever: query_dependencies' 3/10 cutoffs
    pre-dated the PRD and didn't match the AC's literal 5/20 spec.
    Pre-1.0, the cleanest resolution is one helper, one set of
    cutoffs — bumping the file-level thresholds along with the new
    symbol-level field.  If we ever decide file-level needs tighter
    risk classification, the right move is renaming the symbol-level
    helper, not adding a second set of thresholds.
  - `classify_blast_radius` returns `BlastRadius` (the enum), not
    `&'static str` (the AC's signature spec).  The enum already has
    `#[serde(rename_all = "snake_case")]` so it serialises to
    `"low"|"medium"|"high"` automatically, and reusing the enum lets
    Rust callers pattern-match instead of stringly-typing comparisons.
    `blast_radius_serializes_to_snake_case_string` is the regression
    test pinning the JSON contract.
  - When a `PatternResult` is constructed in a control-flow branch
    that returns BEFORE step 7's enrichment runs (e.g. early returns
    on empty queries), the field defaults to `BlastRadius::Low` —
    matching the "0 dependents = Low" semantics.  No need for an
    `Option<BlastRadius>`; the type guarantees a meaningful value.
  - For US-006 (aggregated call_sites by file): the existing
    `enrich_with_call_sites` is the right entry point.  The new shape
    `Vec<{file, site_count, lines, first_snippet}>` will need a
    pre-cap aggregation pass: walk `function_calls` once, group by
    file into a `HashMap<String, FileAggregate>` (count + min_line +
    snippet for the min-line entry), and only then convert into the
    output `Vec`.  `total_call_sites` becomes the sum of `site_count`
    values, replacing today's `call_site_count`.  Be careful with the
    existing `MAX_CALL_SITES_PER_PATTERN` cap — when aggregated by
    file, the cap should probably move to the *file* dimension
    (truncate the list of files, not the list of sites within each
    file), but the AC doesn't specify; check the AC text again before
    deciding.
---
